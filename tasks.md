# PCCA — Refactoring Backlog

[scenarios.md](./scenarios.md) is the product-level source of truth for user/stakeholder intent. This backlog tracks known gaps between the current implementation, that product intent, and the design in [architecture.md](./architecture.md). Each task has a priority, a concrete problem, a sanctioned solution, acceptance criteria, and the impact of fixing it.

**Rules:**
- P0 tasks block daily-use dogfood. Do not start P1+ work while P0 items are open.
- New features land only after the related P0 tasks are closed.
- Every non-trivial PR should reference an existing task ID. If no task exists, add one here first.
- All open tasks must be added as rows in the `Refactoring Tasks` table. Never add standalone task blocks above, below, or outside the table.

**Priority semantics:**
- **P0** — critical correctness / reliability; user-visible silent failure today.
- **P1** — required for 14-day-use stability; spec-mandated reliability controls.
- **P2** — quality of outcomes; makes the curation actually learn / explain.
- **P3** — polish, consistency, observability, nice-to-haves.

**Recently closed (see git history for implementation):** T-01, T-02, T-04, T-05, T-06, T-11, T-13, T-22, T-24, T-28, T-30, T-31, T-34, T-36, T-37A, T-37B, T-03, T-44, T-45, T-46, T-48, T-55, T-56, T-57, T-58, T-43, T-67, T-68, T-70, T-72, T-73, T-74, T-75, T-76, T-77, T-78, T-79, T-81, T-82, T-83, T-84, T-85, T-86, T-87, T-88, T-89, T-90, T-91, T-92, T-96, T-98, T-99, T-100, T-101, T-102, T-107, T-110, T-111, T-112.

---

## Refactoring Tasks

| # | Priority | Problem | Solution | Acceptance | Impact |
|---|---|---|---|---|---|
| T-07 | P1 | No crawl throttling or jitter. Every collector uses fixed `page.wait_for_timeout(N)`; no per-platform rate limits; no randomized delays. Spec [architecture.md §8.1](./architecture.md#8-reliability-and-safety-controls) requires these. Browser scraping at a predictable cadence is a ban signal on X and LinkedIn. | Introduce `CrawlThrottle` utility: randomized wait `base_ms + jitter_ms * random()` between requests; per-platform rate budget (max N page loads per minute). Collectors accept a throttle instance via constructor. Defaults live in `config.py`. | (1) Timing between successive `goto()` calls on the same platform varies within the configured band. (2) Breaching the per-platform budget queues the next call rather than firing immediately. (3) Configurable via env vars. | Reduces ban risk on X / LinkedIn and matches spec §8.1. |
| T-08 | P1 | No circuit breaker on failing sources. Spec [architecture.md §8.1](./architecture.md#8-reliability-and-safety-controls) requires one; one bad source retries every night forever. | Track per-source failure streak in `sources` (new columns: `consecutive_failures`, `suspended_until`). On N consecutive failures (default 3), set `suspended_until = now + 24h` and skip. Auto-resume when expired. Surface suspensions in Telegram `/stats` and digest footer. | (1) A source that 3x raises is not crawled on the 4th run; is re-tried 24h later. (2) `/stats` lists suspended sources. (3) Fresh success clears the streak. | Stops the "same broken source retried 365 times a year" waste and signals real issues to the user. |
| T-09 | P1 | Single shared `aiosqlite.Connection` across pipeline, repositories, and Telegram. aiosqlite serializes per-connection; long pipeline runs block Telegram. WAL mode is not enabled, so writer / reader contention is worst-case. | Enable `PRAGMA journal_mode=WAL` on init. Split connections: one reader pool for services / Telegram, one writer for the pipeline. Repositories accept an injected connection. | (1) Telegram `/read_content` issued while a pipeline run is in flight responds in ≤2s. (2) Migrations confirm WAL is active. (3) No "database is locked" errors under the pipeline-plus-Telegram concurrency test. | Removes the DB contention trap before the first real daily run. |
| T-10 | P1 | Dedup is `(platform, external_id)` only. Same Substack post surfaced via RSS and via a LinkedIn share becomes two distinct items. [scenarios.md §Scenario 3.1](./scenarios.md#scenario-31-system-collects-fresh-content-from-sources) says the system should avoid repeatedly treating the same content as new. | Add `content_fingerprint` column to `items` (SimHash or shingled-hash over normalized text; `canonical_url_hash` as tiebreak). On upsert, look up by fingerprint across platforms; merge metadata if match. | (1) Fixture with the same body text on RSS + LinkedIn produces one `items` row with two `source_id` references in metadata. (2) Fingerprint comparison is O(1) amortized via index. | Eliminates cross-platform duplicate digest items. |
| T-11 | done | Segment-level filtering missing. `item_segments` table is defined but unpopulated. 90-minute podcast transcripts and long-form articles are scored as a single blob. Spec [architecture.md §6.3](./architecture.md#63-segment-level-filtering) requires per-segment scoring. **User feedback (2026-05-01) elevates this from "long-form polish" to "the actual product":** *"in case of podcasts or longer videos i'm not interested in the full content item — just the paragraph/fragment where a valuable piece of information is explained. ideally a deep-link to the starting moment; at minimum, the time offset in the brief metadata."* This reframes the data model: the unit of curation is a **segment**, not an item. Items still exist as containers (one item = one episode/video/post) but the relevance score that drives Brief ranking is `max(segment_scores)` or a weighted top-K, and the Brief renders the specific segment, not the whole-item summary. **This is also the architecturally correct fix for the AI Tools & Tips inversion documented in T-98/T-99 audit:** Darth Vader of Electric Utilities almost-tied Anthropic Claude Code at the item level (0.758 vs 0.757) because the practicality signal averages a 60-min climate piece's body text against a shorter AI transcript. At the segment level, Darth Vader has zero segments scoring high for AI Tools & Tips while Anthropic videos have consistently high segments. `max(segment_scores)` flips the ordering by margin — and the user gets the specific 3-minute fragment they actually want, not a whole-episode summary they have to skim. | **Segmenter** (`pipeline/segmenter.py`): split transcript / long-form by ~300-token window with 50-token overlap; preserve **start_seconds / end_seconds** from the source data when available (YouTube transcripts already include `start` per-row, Apple Podcasts chapter markers when present, Spotify episode chapter markers). Persist into existing `item_segments` table with `(item_id, start_offset_seconds, end_offset_seconds, text, embedding_json, embedding_text_hash)` columns. **Scoring** (`pipeline/curation.py` + `orchestrator`): Pass-1 embedding similarity per segment; Pass-2 LLM rerank operates on segments, not items; item final score = `max(segment_scores)` for ranking + the top segment's text/range carried through to Brief rendering. **Brief rendering** (`digest_renderer.py` + `telegram_service.py`): when delivering an item that has a top segment, the Brief body quotes the segment (not the whole-item description); and emits a **deep-link to the timestamp** when supported per platform: YouTube → `?t=NNs`, Spotify → `?t=NN:NN`, web articles → fragment anchor if available. When deep-linking isn't supported (Apple Podcasts web, generic RSS), include the timestamp as `[NN:NN-NN:NN]` prefix in the Brief body so the user can seek manually. **YouTube transcript service** (`services/youtube_transcript_service.py`): preserve `start` / `duration` per row in the transcript output instead of joining-and-discarding; segmenter consumes the row list directly. **Backfill**: existing items get re-segmented + re-scored on first nightly after T-11 ships; same text-hash invalidation pattern T-92 uses. — **MVP scope decisions** (added 2026-05-01 to remove ambiguity for the implementer): **(i) Aggregation**: `item.final_score = max(segment.final_score)` for ranking. The top segment's text + start/end offset becomes the Brief body. Weighted top-K is filed as a future option but MVP is `max()` — exactly right for "1 valuable minute in a 90-min episode." **(ii) Pass-2 budget**: LLM rerank operates on **the top-1 segment per top-100 candidate item** (not per-segment). Rerank cost stays at ~100 LLM calls per subject — same as today. Pass-1 embedding similarity is computed per segment because embedding is cheap; rerank is per-item-best-segment because LLM calls are expensive. **(iii) Schema**: `item_segments` already exists per [architecture.md §6.3](./architecture.md#63-segment-level-filtering); extend with `embedding_json`, `embedding_text_hash`, `embedding_model`, `embedding_updated_at` (mirrors `items.*` per T-87). New `item_segment_scores` table parallel to `item_scores` keyed on `(segment_id, subject_id)`. `item_scores.final_score` becomes a derived value: `MAX(iss.final_score)` over the segments of that item for that subject. Backward compat: existing `item_scores` rows retain their values until rescore. **(iv) Short-item bypass**: if `len(transcript_or_text) < 600 words`, persist as a single segment with `start_offset_seconds=0, end_offset_seconds=NULL`. X tweets, LinkedIn posts, RSS short-form skip segmentation entirely. **(v) Segmentation source per platform**: prefer `items.transcript_text`; fall back to `items.raw_text`; fall back to single-segment if both <600 words. **(vi) Per-platform deep-link generation** (extend `digest_renderer.py`): `youtube`: `?t=<int>s` ✓; `spotify`: `?t=<MM:SS>` (verify in implementation); `apple_podcasts`: no deep-link support — emit `[MM:SS-MM:SS]` body prefix; `x`/`linkedin`: single segment, no timestamp; `substack`/`medium`/`rss`: no timestamp in MVP — deep-link to section anchors filed as future work; `reddit`: same. **(vii) Segmentation algorithm**: word-count based, 300-word window with 50-word overlap, aligned to sentence boundaries. For YouTube transcripts (already row-segmented at sentence boundaries with per-row `start`/`duration`), combine consecutive rows greedily until ≥250 words OR cumulative duration ≥180 seconds (whichever first); `start_offset_seconds` = first row's `start`, `end_offset_seconds` = last row's `start + duration`. **(viii) Backfill cost & strategy**: reuses T-90 pattern via `pcca embed-backfill --include-segments`. For 1,634 items × ~10-30 segments each = ~16k-49k embeddings, expect 30-90 min at `PCCA_EMBEDDING_BACKFILL_CONCURRENCY=2` (T-100). One-time cost; subsequent nightlies only embed new items' segments. Resumable via segment-level text-hash check (skip already-embedded segments). | (1) A 90-min YouTube transcript produces ≥10 segments persisted in `item_segments` with non-null `start_offset_seconds`. (2) Item-level final score = `max(segment_scores)`; a Darth Vader episode whose 30 segments all score <0.55 ranks below an Anthropic video whose top segment scores 0.72 — even when item-level practicality favors Darth Vader. (3) Brief body for a YouTube item includes a deep-link `https://youtube.com/watch?v=XXX&t=NNs` that opens at the matched segment's start. (4) Brief body for an Apple Podcasts item without deep-link support carries `[14:32-17:08]` timestamp prefix in the body. (5) The user's specific "Bill Gurley episode where 3 min are gold" case: only the top-scoring segment surfaces in the Brief, with timestamp; the rest of the episode never appears. (6) Live regression: recreating today's `AI Tools & Tips` rescore under T-11 puts Anthropic content above Darth Vader at the item level by ≥0.05 final-score margin. | Closes the largest remaining quality lever in the curation pipeline. T-87 fixed *intent capture*; T-92/T-98/T-99 fixed *signal composition*; T-11 fixes the *unit of evaluation*. Without T-11, the system is stuck recommending whole episodes when the user wants the 3-minute fragment that mattered — that's not a polish concern, it's a product mismatch. **Promotes T-11 from "long-form polish" to the next major piece of work.** Once T-11 lands, the practicality length-bias documented in the T-98/T-99 audit becomes structurally moot (segments are short and similarly-sized; practicality stops being a length proxy). |
| T-12 | P1 | Collector contract is loose and inconsistent. Four different names for max-items (`max_posts_per_source`, `max_videos_per_source`, `max_entries_per_source`, `max_episodes_per_source`); defaults vary 8 / 12 / 20 without rationale. `published_at` is null for LinkedIn / YouTube / Spotify. YouTube sets `author = source_id` (channel ID, not human name). Spotify captures title only. | Introduce a shared `CollectorConfig` (max_items, crawl_delay, user_agent). Rename per-collector fields to `max_items`. Fix field capture: YouTube `author` from channel name element; LinkedIn `published_at` from time element; Spotify description from show page; YouTube `published_at` from video metadata. | (1) Every collector exposes the same `max_items` param. (2) On a live fixture, `published_at` is non-null for all nine platforms. (3) YouTube digest reads "via <Channel Name>" not "via UCxxxx". | Makes cross-platform behavior predictable and fixes user-visible digest UX regressions. |
| T-14 | P1 | Browser contexts are never recycled. If a page hangs mid-scroll, the cached context is re-used next call; no watchdog, no close-on-error. | Wrap each collector's page operation in a timeout watchdog; on timeout, close the context and recreate on next call. Track context age and rotate after N hours. | (1) A hanging `goto()` returns an error within timeout and does not pin the page. (2) Context restart is observable in logs. (3) A stuck LinkedIn infinite-scroll recovers on the next run. | Eliminates "pipeline stalls and nothing ever recovers" scenarios. |
| T-15 | P1 | `asyncio.create_task` references throughout the codebase are not retained (see `scheduler.py`, manual actions). Event loop can GC them before they complete. | Maintain an app-level `_background_tasks: set[asyncio.Task]` held by `PCCAApp`; each `create_task` adds itself and registers a `done_callback` that removes itself and logs exceptions. | (1) Grep for raw `asyncio.create_task` returns only the one app-managed helper. (2) No `RuntimeWarning: coroutine was never awaited` in test or production logs. | Prevents silent disappearance of background work. |
| T-16 | P2 | Curation is keyword-list-only. `practical_terms` is 20 hardcoded entries; `novelty` is a constant 0.8 with a crude fallback; `trust` is a constant with +0.1 for X/LinkedIn authors. Ollama reranker only adjusts a delta. With T-46's rich rule schema in place, the conditional rules (e.g. "exclude Elon Musk unless space") can only be evaluated by an LLM that reads them in context — a keyword scorer cannot. | Promote `ModelRouter` to primary Pass-2 scorer on shortlisted items. Keyword scorer stays as ultra-cheap Pass-1 pre-filter on titles + author / source flat lists from [architecture.md §5.3](./architecture.md#53-preference-rule-schema). Pass-2 prompt receives the subject's full structured rules (including `conditional_rules`) verbatim alongside the item under evaluation, returns structured JSON (`relevance`, `practicality`, `novelty`, `trust`, `noise`, `rationale`, `rule_decisions[]`). Budget controls: max Ollama calls / run, per-call timeout, fallback to keyword if budget exceeded. | (1) Pass-2 scores for a 20-item fixture are produced by Ollama and stored with rationale. (2) Running with Ollama disabled falls back to keyword Pass-1 shortlist only, not empty digest. (3) Token / call budget is configurable via env. (4) Fixture: subject with `conditional_rules: [{kind: exclude_unless_topic, author: elon musk, unless_topics: [space, spacex]}]` correctly filters a Tesla item but keeps a Starship item. (5) `rule_decisions[]` captures which conditional rules fired per item for explainability. | Replaces the toy scorer with something that can actually discriminate practical vs generic *and* can enforce the rich rule shapes the user states explicitly. |
| T-17 | P1 | Feedback is write-only and the planned interpretation is too deterministic. Today, `feedback_events` is never read for learning; the original T-17 plan ("EMA of pos/neg → adjust source weight") locks in one interpretation of every reaction. A 🚫 tap can mean "spammy source" / "spammy specific post" / "spammy original author repost" / "off-topic for THIS subject" — not just "downgrade source." Per [architecture.md §3.2](./architecture.md#32-explicit-vs-implicit-feedback) and [§4.20](./architecture.md#420-learning-strategy), interpretation belongs in a swappable layer, not hardcoded. **Note on the input shape after T-55**: button taps now record `feedback_type='button_macro'` with `comment_text=<text_macro>` (e.g., "this is spam or off-topic"). The Learning Strategy is the only thing that parses that macro string into a typed signal — there is no longer a distinct `feedback_type='button_down'` enum to switch on. | Define `LearningStrategy` interface ([architecture.md §4.20](./architecture.md#420-learning-strategy)) — `interpret(feedback_event, item, subject, recent_history) -> list[LearningAction]`. Action kinds: `no_action`, `ask_user_to_disambiguate`, `adjust_source_weight`, `adjust_author_weight`, `adjust_topic_terms`, `suspend_source_for_subject`, `tag_item_for_review`. Ship a default conservative implementation: threshold-gated (most actions require ≥3 similar reactions in 14 days), and emits `ask_user_to_disambiguate` when a pattern is visible but ambiguous. The bot posts the disambiguation question; the user's reply becomes its own feedback event. Strategy reads `feedback_events` + `subject_preferences`, writes proposed actions back into versioned preference rows and `subject_sources` overrides. Decisions logged to `run_logs(run_type='learning_run')`. **Macro parsing**: the default strategy reads `feedback_events.comment_text` and matches against the configured button shortcuts (default: "more like this" → positive, "less like this" → negative, "save this brief" → bookmark, "this is spam or off-topic" → spam, plus any per-subject overrides from `subjects.button_shortcuts_json`). Free-form text replies (`feedback_type='reply_text'`) are passed to the same macro classifier with an LLM fallback when Ollama is enabled. **Invariant**: explicit user instructions ([architecture.md §4.10.1](./architecture.md#4101-preference-extraction-service-planned-task-t-46)) always take precedence over Learning Strategy proposals. | (1) `LearningStrategy` interface defined; PCCA registers exactly one strategy at startup with the default conservative implementation. (2) A 🚫 reaction below threshold produces `no_action` (recorded only); at threshold the strategy proposes either a `suspend_source_for_subject` action or an `ask_user_to_disambiguate` follow-up depending on signal clarity. (3) After 5 consecutive 👎 (i.e. 5 events with `comment_text="less like this"`) on items from source S in subject X, the strategy suspends S for X (not globally) and surfaces the suspension in the next Brief footer. (4) An external contributor can replace the default strategy by registering a different `LearningStrategy` instance — no other code changes required. (5) `run_logs(run_type='learning_run')` records the actions taken per run. (6) Disambiguation prompts post a Telegram follow-up message and route the user's reply back through the same feedback path. (7) The default macro classifier handles user-relabelled buttons via `subjects.button_shortcuts_json` (e.g. a user changing "🚫" to "spam: source") without code changes. | Closes the learning loop without locking in a single interpretation model. Makes feedback interpretation a first-class extension point alongside `DigestRenderer` and `ModelRouter`. Bumped from P2 to P1 because without it, button reactions are write-only and the user's "less of this" feedback never affects future briefs. |
| T-18 | P2 | No wake-aware scheduling. Spec [architecture.md §7](./architecture.md#7-scheduling) requires catch-up on resume. APScheduler's `misfire_grace_time` isn't configured; no `caffeinate` / power-request integration. | Configure `misfire_grace_time`. On app start, compute "last successful nightly run at" and fire a catch-up if > 1 nightly window has been missed. Wrap the nightly job in platform-specific keep-awake (`caffeinate` on macOS, `SetThreadExecutionState` on Windows). | (1) Closing the laptop during the nightly window and reopening in the morning triggers the missed run. (2) Machine doesn't sleep mid-nightly-job. (3) Two misses in a row produce a single catch-up, not two. | Prevents silently-missed days from undermining the "nightly habit." |
| T-19 | P2 | Intent parser is regex-only. Spec [architecture.md §2 decision 5](./architecture.md#2-product-decisions) says "free form by default, not command syntax dependent." ~13 regex patterns = command syntax with fuzzy edges. | Add an LLM fallback (Ollama) for text that doesn't match any pattern. Prompt returns a typed `IntentAction` + slots. Regex stays as fast-path; LLM catches the long tail. Disabled if Ollama is off. | (1) "Start tracking Rob Lee on LinkedIn for my PM subject" is correctly parsed as `ADD_SOURCE`. (2) Regex path unchanged for existing phrasings. (3) LLM parse is logged with a rationale; a `/explain-last` command dumps it. | Makes "free form by default" actually true. |
| T-20 | P2 | Observability is skeletal. Only `run_logs.stats_json` today. Spec [architecture.md §4.13](./architecture.md#413-observability) calls for per-collector extraction success %, model latency, digest delivery success. | Add `metrics` table (or JSON rollups in `run_logs`): per-run, per-collector `items_attempted`, `items_extracted`, `session_challenged`, p50/p95 latency; model-router `calls`, `successes`, `timeouts`; digest `sent`, `failed`. Expose via Telegram `/stats` and a CLI dump. | (1) `/stats` returns a week summary within 1s. (2) Every metric is derivable from a single `run_logs` query. (3) Missing-metric regressions fail a unit test. | Makes problems visible instead of waiting for user to notice. |
| T-21 | P2 | Digest delivery has no retry. Transient Telegram errors drop the digest for the day. | Wrap `bot.send_message` in exponential-backoff retry (3 attempts, 2s → 8s). Persist `message_id` on success; record failure in `digests.status`. Daily digest is resilient to a single Telegram hiccup. | (1) Injected 5xx on first send completes successfully on retry. (2) Three consecutive failures mark the digest `failed` and log an actionable message. | Prevents a bad-network-moment from becoming a missed day. |
| T-23 | P3 | Voice transcription is a documented placeholder. `/help` copy advertises voice support; the service always returns `None`. | Ship local Whisper integration (`openai-whisper` or `faster-whisper`) behind an env flag. If disabled, reply with a "voice not configured" message that links to setup docs. Remove the "backend pending" wording from `/help` when enabled. | (1) With Whisper enabled, a voice note is transcribed and passed through `intent_parser`. (2) With Whisper disabled, `/help` does not claim voice support. (3) Transcription happens locally — no outbound call by default. | Delivers on the voice-first promise the spec makes. |
| T-25 | P3 | Platform-support surfaces are out of sync. `intent_parser.PLATFORMS` accepts `reddit` and `rss`; `FollowImportService.supported_platforms()` does not. User can `add source reddit:foo` but cannot `import-follows --platform reddit`. | Define platforms in one authoritative registry (e.g., `platforms.py` with capability flags: `supports_follow_import`, `requires_login`, `has_rss`). Every consumer imports from there. CLI / Telegram / discovery derive their UX from it. | (1) Adding a platform to the registry automatically lights up CLI help, Telegram parser, and `import-follows` eligibility. (2) Reddit and RSS have correct capability flags (no `import-follows` for either). (3) A unit test asserts consistency. | Eliminates the class of bug where four files must agree and they don't. |
| T-26 | P1 | Browser profiles and captured session material are sensitive bearer credentials. T-37A avoids storing raw cookies in PCCA SQLite, but Playwright browser profiles still hold authenticated sessions on disk. Spec [architecture.md §9](./architecture.md#9-security-and-privacy) mentions encrypted-at-rest config. | Add OS-keychain-backed encryption/secret management for captured sessions and profile state where feasible; add explicit session clearing per platform; redact cookie/session values from all logs and UI. macOS Keychain / Windows Credential Manager / libsecret on Linux. | (1) Raw captured cookie values are never written to logs or SQLite. (2) User can clear a platform session from CLI/wizard. (3) Any PCCA-owned session secret is protected by OS keychain or the browser profile's native cookie encryption. (4) Losing keychain/profile access forces re-capture but does not corrupt DB. | Security becomes core once PCCA imports browser sessions; this is no longer polish. |
| T-29 | P1 | T-12 follow-up — YouTube and Spotify `published_at` still null. T-12 unified the `max_items` knob and fixed YouTube author + LinkedIn `published_at`, but YouTube and Spotify collectors still emit `published_at=None`. Digest renders "published: unknown" for ~2 of 9 platforms and time-aware ranking (T-07 pacing, T-18 catch-up, T-27 weekly reflection) degrades on those items. | YouTube: parse `yt-initial-data` JSON blob on the channel Videos page, or per-video `itemprop="datePublished"` meta on the watch page; only pay the per-video fetch for shortlisted items once T-03 lands. Spotify: read the `time[datetime]` element inside each episode container on the show page. Persist as ISO-8601 in `items.published_at`. | (1) Live fixture: YouTube channel with 5+ videos yields non-null `published_at` for every returned row. (2) Live fixture: Spotify show with 5+ episodes yields non-null `published_at` for every returned row. (3) Digest line "published: unknown" does not appear for `platform in ('youtube', 'spotify')`. (4) Unit test on a static HTML fixture asserts parse. | Completes T-12 acceptance (2) across all nine platforms and restores time-based signals for two of the three highest-volume content types. |
| T-35 | P3 | T-34 follow-up — Linux desktop support is deferred. PyWebView on Linux requires `gir1.2-webkit2-4.0` (GTK + WebKit2GTK) installed via `apt`/`dnf`, which violates the "pip install only" bar T-34 holds for Mac/Windows. T-34 ships an explicit "not yet supported on Linux; use the CLI" message rather than a half-broken Tk fallback. | Add Linux support to the desktop shell once the broader UX is stable. Two viable approaches: (a) PyWebView + WebKit2GTK with documented `apt install gir1.2-webkit2-4.0` step (one extra command but real native window), or (b) browser-tab fallback via stdlib `webbrowser` (zero extra install but the wizard appears as a browser tab, not a native window — same pattern as Jupyter / Streamlit / MLflow). Pick one or implement both with a flag. | (1) After `pip install -e .` on a typical Ubuntu/Fedora/Arch desktop with the documented prereq (if any), `pcca run-desktop` opens an interactive wizard with all six steps reachable. (2) Failure modes are explicit: missing prereq prints the exact `apt`/`dnf` command. (3) Tests cover both modes if both are shipped. | Brings Scenario 1 testing to Linux developers / dogfooders. Lower priority than Mac/Windows because the user base for v1 is macOS-first; Linux is a "should also work" tier. |
| T-37A | done | Automated login through Playwright-controlled Chrome is not reliable enough for Scenario 1. X/Cloudflare/Google can reject it below the JavaScript layer, and failed attempts can poison reputation. We need the first narrow replacement path before dogfooding. Code implementation has landed; this remains open only for live local Chrome/Arc capture verification. | Implement the first vertical slice of native-browser session import: read X cookies (`auth_token`, `ct0`, optional `twid`) from macOS Chromium-family browser stores (Chrome/Arc/Brave/Edge), decrypt via Keychain + Chromium cookie format, copy DBs before reading to avoid locks, inject cookies into PCCA's Playwright profile, and expose `pcca capture-session --platform x` plus the desktop `Get Sources` inline session-repair path. No raw cookie values in logs or SQLite. | (1) Unit tests cover cookie DB parsing, HttpOnly cookies, domain filtering, and injection shape. (2) CLI reports captured cookie names only, never values. (3) Desktop wizard can trigger capture for X as an inline prerequisite/repair step. (4) Live macOS Chrome/Arc capture can be verified by user with `pcca capture-session --platform x` auto-detecting the browser with X cookies, or with an explicit `--browser arc/chrome`, and then `pcca stage-follows --platform x`. (5) PCCA does not drive X login as the primary user path. | Unblocks a realistic Scenario 1 smoke test for X without fighting Cloudflare login. This is the smallest proof that the new architecture works. |
| T-37C | P1 | T-37A only proved X. Scenario 1 still needs equivalent session capture for LinkedIn, YouTube, Spotify, Substack, Medium, and Apple Podcasts before multi-platform dogfood is reliable. Code implementation has landed; this remains open for live local verification across platforms. | Extend platform cookie targeting and capture validation: LinkedIn = `li_at` plus optional `JSESSIONID`; YouTube = Google SID/SAPISID-family cookies; Spotify = `sp_dc` plus optional `sp_key`; Substack = `substack.sid`; Medium = `sid`/`uid` where available; Apple Podcasts = best-effort Apple web auth cookies. Each platform maps cookies to the correct Playwright domains and has fixture tests. | (1) Fixture tests cover all target cookie names/domains. (2) Capture result reports missing required cookies per platform. (3) After capture, follow import for each supported platform uses injected cookies and does not open a login flow. (4) Live local capture is verified for each platform the user wants to dogfood. | Expands the realistic session model from X proof-of-concept to the full logged-in source set. |
| T-37D | P1 | Windows users need the same native-browser session import, but Chromium cookies use DPAPI + AES-GCM via the browser `Local State` key, not macOS Keychain/AES-CBC. | Implement Windows Chromium cookie reader for Chrome/Edge/Brave/Arc if present: read encrypted key from `Local State`, decrypt with DPAPI, decrypt cookie `encrypted_value` with AES-256-GCM, copy locked DBs before reading, and feed the same injection pipeline. | (1) Windows Chrome captures X `auth_token` and `ct0` without admin rights. (2) Missing browser/profile/key errors are actionable. (3) Unit tests cover Local State parsing and AES-GCM fixture decryption. | Gives the same Scenario 1 path to Windows users without changing product UX. |
| T-37E | P1 | Bookmarklets cannot read HttpOnly cookies, so they are not a reliable fallback for X/LinkedIn/Google auth. Users outside Chromium/macOS/Windows still need an escape hatch. | Replace the bookmarklet fallback with a realistic fallback menu: browser extension prototype using browser cookie APIs, power-user CDP attach (see T-39), and/or manual cookie export/import for developer dogfood. Bookmarklet may remain only for non-HttpOnly diagnostic cookies and must be labeled limited. | (1) No acceptance criterion depends on `document.cookie` reading HttpOnly cookies. (2) At least one fallback works for Safari/Firefox or documented developer workflows. (3) Security warnings are explicit for extension/CDP/manual import paths. | Avoids promising an impossible fallback while still keeping non-Chromium users from being stranded. |
| T-38 | P1 | T-37 follow-up — Safari and Firefox cookie readers. Chromium capture plus T-37E fallback covers most users, but Safari/Firefox Tier-1 readers would remove friction and reduce support load. | Safari: parse `~/Library/Cookies/Cookies.binarycookies` where auth cookies are available and OS permissions allow access. Firefox: SQL query against profile `cookies.sqlite`. Auto-detect default browser where safe; manual override via `PCCA_BROWSER_FOR_CAPTURE`. Both feed the same injection pipeline. | (1) Safari reader captures real platform auth cookies when they are present and accessible. (2) Firefox reader captures real platform auth cookies when they are present and accessible. (3) HttpOnly cookies are tested explicitly; ordinary cookie-only success is not enough. (4) Permission failures are actionable. | Closes the non-Chromium convenience gap after the main capture architecture is proven. |
| T-39 | P2 | T-37/38 follow-up — power-user CDP attach. Some advanced users want PCCA to drive their actual running browser instead of capturing cookies; uses include debugging anti-bot behavior and avoiding repeated cookie capture. This is not a consumer default. | Add `pcca chrome-attach` command and a wizard developer toggle. User launches Chromium with `--remote-debugging-port=9222` or PCCA spawns an isolated instance with that flag. `BrowserSessionManager` gains `attach_to_running_browser(port)` mode using `chromium.connect_over_cdp(...)`. Scraping happens through the attached browser; detach cleanly and never quit the user's browser. | (1) `pcca chrome-attach --port 9222` connects to a running Chromium. (2) Scrape collectors run through the attached browser and match cookie-injection results. (3) Disabled by default; security warning shown: any local process can attach to this debug port. (4) Docs label it power-user/debug mode, not recommended onboarding. | Useful fallback/debug path, but not required for the normal user-facing scenario. |
| T-42 | P1 | T-37/T-40 follow-up — capture iterates every Chromium-family browser even when only one is in use. With `Auto` selected the SessionCaptureService tries Chrome → Arc → Brave → Edge in order. Each browser that's installed but missing the platform's required cookies triggers its own macOS Keychain prompt before the loop continues. A user on Arc-only sees ~2 prompts per platform on first capture; with all four browsers installed (or platforms like Apple Podcasts where required cookies are loose and many browsers partially match), it can be 4–5 prompts. Confirmed live: log shows Chrome attempted first, Arc selected after the second prompt. | Add macOS-default-browser detection (`defaults read com.apple.LaunchServices/com.apple.launchservices.secure LSHandlers`-style or simpler: probe which Chromium-family browser is currently running). When the wizard sends `browser=auto`, prefer that detected default and skip the others unless it lacks the required cookies. Manual override via the existing dropdown stays. T-40's refresh path should consume the same default-browser hint so subsequent re-reads also avoid prompting browsers the user doesn't use. Caching at the App level (not per `SessionCaptureService` instance) is also useful so repeated capture+refresh in one session never re-prompts a browser already cleared. | (1) On a machine with Chrome, Arc, Brave, Edge all installed but only Arc logged in to X, capturing X with `Auto` produces exactly **one** Keychain prompt (Arc), not four. (2) Manual `--browser chrome` still tries Chrome regardless. (3) Once "Always Allow" is granted for Arc Safe Storage, no further prompts during normal session capture/refresh in subsequent runs. (4) Logged: `Detected default Chromium browser=arc`. | Eliminates the "why does PCCA prompt the keychain 4 times?" friction the user hit on Apple Podcasts capture. Combined with the user clicking "Always Allow" once per actually-used browser, capture and T-40 refresh become invisible. |
| T-43 | done | Staged sources show opaque IDs in the wizard's review panel: LinkedIn rows display the bare URN (`ACoAAAX15tQBXDaay4QkwgQppm4BvCOv2LunOA0`); Apple Podcasts rows all read `apple-podcast-feed`. The user has no signal which person/podcast each row represents, so meaningful curation in step 4 ("review staged sources") is impossible. The data is available — `SourceDiscoveryService._lookup_apple_podcast_feed` already calls iTunes' lookup API which returns `trackName`/`collectionName`, and the LinkedIn follow page renders the human name next to each profile link — both are just being thrown away. | (a) `SourceDiscoveryService` for Apple Podcasts: persist the iTunes-lookup `trackName` (or `collectionName`) as the `display_name` instead of the hardcoded "apple-podcast-feed". (b) `FollowImportService.import_linkedin_follows`: extend the JS evaluator to capture the visible name alongside the URN — `link.closest('div').querySelector('span[aria-hidden="true"]')?.innerText` or similar — and pass through as `display_name`. Same approach for X (handle is OK but display name from `<span>` would be nicer) and YouTube subscriptions (channel name is right next to the link). | (1) Apple Podcasts staged rows render the actual show title (e.g. "Acquired", "This American Life") rather than `apple-podcast-feed`. (2) LinkedIn staged rows render the user's name (e.g. "Andrej Karpathy") rather than the URN. (3) X and YouTube continue to show their existing display names. (4) Existing data is back-filled on next stage-follows or via a one-shot migration. | Step 4 of the wizard ("review staged sources") becomes actually usable. Without this the user either confirms 87 mystery rows blindly or removes them all — neither is real curation. |
| T-47 | P1 | Per [scenarios.md §3.1.4](./scenarios.md#scenario-31-system-collects-fresh-content-from-sources), the system should collect "descriptions, authors, timestamps, views, likes, shares, comments, reposts, and other signals that may help identify trending, breaking, reputable, or debatable content." Today only basic fields (title, author, text, URL, published_at, transcript_text) are captured; engagement signals are invisible to the curation engine. | Per-platform engagement signal capture into `items.metadata_json` (the column already exists). Concrete signals: X — `like_count`, `repost_count`, `reply_count`, `view_count` from `[data-testid="like"]` / `repost` / `reply` aria-labels and `[data-testid="app-text-transition-container"]` for views; LinkedIn — reactions count, comment count from the social-counts row; YouTube — `viewCount`, `likeCount` from `ytInitialData` JSON, video duration in seconds; Spotify — episode length; Reddit — `score`, `num_comments` (already partially captured). Plumb into Pass-1 / Pass-2 as scoring inputs (`engagement_velocity`, `engagement_outlier`) so "trending / breaking / debatable" become real signals instead of constants. | (1) Live fixtures for X/LinkedIn/YouTube produce non-null engagement counts in `items.metadata_json`. (2) The curation engine reads them through a typed accessor with safe defaults. (3) `engagement_velocity` (signal change vs. account/channel baseline) is observable in `rationale_json`. (4) Existing rows back-fill on next collection or via a one-shot migration. | Lifts the latent signal that PCCA's whole pitch depends on into the ranking pipeline. Without it, "trending vs. evergreen", "controversial vs. consensus", "breaking vs. catalogued" are all guesses. |
| T-59 | P2 | **Repointed by T-87 (2026-05-01) as the explicit-override layer above the embedding-first scorer, not the primary scoring mechanism.** T-46 ships a basic preference-extraction LLM call that handles flat include/exclude topic lists, but does not yet emit author-level rules, source-level rules, or `conditional_rules` per [architecture.md §5.3](./architecture.md#53-preference-rule-schema). Live examples that should work but don't yet: *"Ignore Elon Musk unless he posts about space"* (conditional carve-out on author), *"I do not want any content about Cursor"* (topic exclusion — works today), *"Don't show me anything from @yourdailyAI"* (per-subject source suppression). **Live regression (2026-04-30 user test):** rich free-form description — *"updates should come from companies leading in AI domain, from their employees, from thought leaders... but i do not want random guy on linkedin sharing his claude.md, skills.md, prompts, or top AI tools for productivity spammy content (even though they sometimes have high engagement)"* — was extracted into flat `include_terms=['practical tips', 'leading ai companies', 'vibe coding', 'ai agents', 'it applications']`, flat `exclude_terms=['personal linkedin posts', 'skills.md', 'prompts', 'engagement spam', 'top ai tools for productivity']`. Three layers of intent were silently lost: **(i) Authority-of-source filter** demoted to topic-keyword `"leading ai companies"` — the curator searches content for that literal string instead of filtering by who authored the post; an authoritative one-line tweet from Karpathy ranks low while a spammer who writes *"top tips from leading AI companies"* ranks high. **(ii) Conditional excludes** turned into flat blocks — `skills.md` / `prompts` / `claude.md` should be excluded *only* from low-authority sources, but the flat exclude penalizes Anthropic's official Skills launch post and Karpathy's prompting threads. **(iii) Engagement-as-anti-signal** has no schema home — user explicitly said "don't trust likes/views" but the curator's trust score still positively weights engagement, so the spammy listicles the user told it to ignore float up exactly because they have engagement. Title compression is also weak (`"AI Tools & Tips"` loses vibe-coding / AI-agents / IT-org-adoption focus). This is not a model-quality limitation — even GPT-5 cannot encode authority/conditional/anti-engagement rules into flat `include_terms`/`exclude_terms` if the scorer downstream doesn't read them as such. **Schema is the bottleneck.** | **Extends the existing T-46 `PreferenceExtractionService` and its `ModelRouter.extract_subject_preferences` prompt** rather than building a parallel pipeline. Output schema grows from `{title, include_terms, exclude_terms, quality_notes}` to the full §5.3 shape: `include_rules.{topics, authors, sources}`, `exclude_rules.{topics, authors, sources}`, `quality_rules.conditional_rules[]`, and a `signal_weights` field for meta-rules like `engagement_weight: "discount"`. Source-scoped rules write into `subject_sources(status='suspended')` rather than the JSON column. T-46's heuristic fallback also extended so the Ollama-disabled path emits the same rule kinds (best-effort). T-46's rephrase-and-validate loop displays the new rule kinds back to the user in plain English ("I'll exclude *elon musk* except when topic is *space, spacex, starship*"; "I'll discount engagement counts when ranking — likes/views won't boost spammy content"). Curation engine (Pass-1 keyword + Pass-2 LLM rerank, T-16) updated to consume `authors` / `sources` / `conditional_rules` / `signal_weights`. Title generation prompt updated to allow up to 7 words and to preserve the most distinctive descriptors rather than picking the two most generic. | (1) Free-form input *"Ignore Elon Musk unless he posts about space"* round-trips through extraction → confirmation → save and produces the right `conditional_rules` entry. (2) *"Don't show me anything from @yourdailyAI"* writes a `subject_sources(subject_id=X, source_id=Y, status='suspended')` row, not a JSON edit. (3) The 2026-04-30 regression input (the user's rich description quoted in Problem) round-trips with: `include_rules.authors=['leading ai company employees', 'ai thought leaders']` or similar; `include_rules.sources` listing major AI-company domains; `exclude_rules.conditional_rules` containing *"exclude posts mentioning skills.md/claude.md/prompts UNLESS author in authoritative-list"*; `signal_weights.engagement_weight='discount'`; title preserving the AI-agents / vibe-coding / practical-adoption focus. (4) Confirmation message renders rich rules in plain English. (5) Pass-2 scoring (T-16) consumes the rich rules and applies them per item — verified by a fixture where a low-authority "skills.md tutorial" is scored low and the equivalent post from Anthropic's blog is scored high. (6) `signal_weights.engagement_weight='discount'` measurably reduces the item-rank correlation with engagement counts (regression test on a synthetic dataset). (7) Heuristic fallback path emits at least author-level and topic-level rules (conditionals and signal_weights are LLM-only). | Closes the gap between Scenarios 2 and 5 (free-form preference statement) and the user's actual phrasing. Without it, anything beyond "include / exclude these topics" requires a CLI fallback. **Also closes the user-visible "is this even a correct interpretation of what i said?" frustration documented in the 2026-04-30 review — schema is the limiter, not the LLM.** |
| T-60 | P1 | Telegram should be a first-class source platform for groups/channels the user is already a member of. Bot API is not suitable because adding a bot to every group/channel changes the social surface and often won't be allowed; browser scraping Telegram Web would be less reliable than the official client protocol. | Add a Telegram source connector based on MTProto user-client access, preferably `Telethon` in the Python stack. This is a separate source-account connection from the existing Telegram bot used for delivery. Add `telegram` to the platform registry with capabilities: `requires_user_auth=true`, `supports_dialog_import=true`, `supports_group_collection=true`, `supports_channel_collection=true`, `bot_delivery=false`. Production path uses PCCA-owned Telegram API credentials; developer path can accept `PCCA_TELEGRAM_API_ID` and `PCCA_TELEGRAM_API_HASH`. | (1) `telegram` appears as a supported source platform in wizard/CLI registry surfaces. (2) The implementation path does not require adding a bot to source groups/channels. (3) Telegram delivery bot token remains separate from Telegram source user-session credentials. (4) Docs clearly state that PCCA reads only chats the user explicitly selects. | Unlocks one of the highest-signal source types for niche communities while avoiding a brittle Telegram Web scraper or socially awkward bot-invite workflow. |
| T-61 | P1 | Telegram user-session auth is sensitive and different from browser-cookie capture. A logged-in MTProto session is powerful; if PCCA handles it casually, the product creates unnecessary account/security risk. | Implement `TelegramUserSessionService`: phone-number login, code confirmation, 2FA password prompt, session persistence under `.pcca/telegram_sessions/`, session health check, and explicit disconnect/delete-session action. Store no Telegram messages in logs during auth. Add a first-run warning: PCCA acts as a local Telegram client on the user's behalf; it will only read selected chats and will never send/join/invite/delete/react. | (1) User can connect Telegram source account from desktop wizard without terminal commands. (2) Existing session is reused without repeated login codes. (3) 2FA accounts work. (4) User can disconnect Telegram and the local session is deleted. (5) Auth failures are actionable and do not corrupt existing sessions. | Makes Telegram feasible for normal users while treating the user-client session with the same seriousness as browser auth cookies. |
| T-62 | P1 | The user needs to select which Telegram groups/channels to monitor. Reading every dialog by default would be noisy, privacy-hostile, and a risk signal. Secret chats and private DMs should not be part of the default source-import flow. | Implement `TelegramDialogImportService`: list dialogs the user is a member of, stage only channels, supergroups, and groups by default; exclude private 1:1 DMs, bots, and secret chats. Persist display name, peer id, peer type, username/public link when available, private/public flag, and access metadata in source metadata. Review/confirm uses the existing global monitored-source flow from T-44/T-45. | (1) Wizard shows Telegram groups/channels with readable titles and type labels. (2) Private DMs, bot chats, and secret chats are excluded by default. (3) User can select/remove staged Telegram dialogs before monitoring. (4) Confirming creates `sources(platform='telegram', is_monitored=true)` rows without binding them to one subject. | Gives the user explicit control over Telegram source scope and keeps Scenario 1's source confirmation model consistent across platforms. |
| T-63 | P1 | Telegram collection must be incremental, rate-limit-aware, and read-only. A bad implementation that scrapes full history, ignores `FloodWait`, marks everything read, or accidentally calls write APIs could create account friction or ban risk. | Implement `TelegramCollector` over selected monitored Telegram sources. Use per-source cursor storage, initially `last_message_id`, to fetch only new messages. Respect Telegram `FloodWait` exactly and back off source/platform collection when hit. Enforce a read-only adapter exposing only dialog/message read methods; no send, join, invite, forward, delete, react, or mark-read calls. Store message ids and minimal sender metadata; do not acknowledge/read-mark messages unless unavoidable and documented. | (1) First run can optionally cap history import; later runs fetch only messages newer than the cursor. (2) Simulated `FloodWait` pauses collection and logs a clear rate-limit event. (3) Unit tests assert the collector cannot call write methods through its adapter. (4) A failed chat does not stop collection for other Telegram sources. (5) No watched Telegram source is crawled more than once per nightly cycle. | Keeps Telegram ban/account-risk low by behaving like a conservative local reader, not a crawler or automation bot. |
| T-64 | P1 | Telegram groups are conversational and noisy; treating every message as an independent content item will flood scoring with low-value fragments. Channels and groups need different normalization. | Add Telegram normalization and aggregation. Channel posts become candidate items directly. Group messages are stored as raw-ish message records but candidate items are built as reply threads, link-share discussions, forwarded-post clusters, or time windows around high-signal bursts. Metadata should include chat title/type, message timestamp, sender display name or stable anonymized sender id, reply count, reactions, views where available, forwards, links, media type, forwarded-from metadata, and public message URL where available. | (1) Channel fixture produces one item per post. (2) Group fixture with 50 short messages produces a small number of thread/window/link candidate items, not 50 digest candidates. (3) Reactions/replies/views are represented in `metadata_json` and readable by the engagement scorer from T-47. (4) Candidate item text includes enough context for Pass-2 but avoids dumping entire private conversations. | Makes Telegram useful for curation rather than just adding another noisy firehose. Especially important for niche groups where the signal is often in discussion clusters, not individual messages. |
| T-65 | P1 | Telegram private groups/channels can contain sensitive user/community content. Debug logs, support bundles, and briefs must not accidentally export private conversations verbatim or make it unclear what is being read. | Add Telegram privacy mode and tests. Defaults: no raw Telegram message bodies in logs/debug bundles; private-source briefs summarize rather than quote long verbatim text; source rows show a clear private/public label; user can pause/remove Telegram sources; support bundle redacts chat titles optionally. Add fake Telethon-client tests for auth, dialog import, incremental cursor reads, FloodWait handling, normalization, disconnect, and no-write-method enforcement. | (1) `pcca debug-bundle` contains Telegram counts/status but no raw private message bodies. (2) Private Telegram source briefs avoid long verbatim quotes. (3) Removing a Telegram source stops future reads without deleting already-collected global items unless the user chooses a separate purge action. (4) Test suite covers the full connector without real Telegram credentials. | Gives Telegram-source support a privacy/safety envelope strong enough for dogfood and eventual production use. |
| T-66 | P3 | T-46 follow-up — `pending_subject_drafts` rows have no TTL enforcement. The architecture (§4.10.1) specifies a 1-hour TTL so abandoned drafts don't accumulate, but the implementation persists drafts indefinitely. Today this is harmless in single-user mode, but it'll silently grow a stale-row tail and surprise a future user who started drafting a subject yesterday and finds the bot still treating their next message as a correction. | Cheapest path: lazy expiration on read. `SubjectDraftRepository.get(chat_id)` checks `updated_at`; if older than `PCCA_SUBJECT_DRAFT_TTL_SECONDS` (default 3600), delete the row inline and return None. No cron or background job needed. Add a complementary `SubjectDraftRepository.purge_expired()` for explicit cleanup that the existing `learning_run` job (T-17) or a tiny scheduler tick can call once per day so old rows don't sit forever even for chats that never message again. Configurable TTL via env var; logged when expiration fires. | (1) A draft created and not touched for >TTL is treated as nonexistent on next message — a fresh `Create subject: …` produces a new draft, not a "you have a pending draft" rephrase. (2) `purge_expired()` removes rows whose `updated_at` is older than TTL and returns the count. (3) Default TTL is 1 hour; configurable via `PCCA_SUBJECT_DRAFT_TTL_SECONDS`. (4) Expiration events are logged at INFO with `chat_id` redacted to a hash. | Closes the only durable bookkeeping gap in T-46. Low priority because single-user impact is small, but worth fixing before multi-user or longer-lived dogfood. |
| T-67 | done | T-57 follow-up — smart `/briefs` rebuild trigger is too narrow. Today the smart path runs the rebuild branch only when `top_unsent_candidates(limit=5)` returns at least one item; otherwise it resends the previous composition with a "no new content" footer. That misses a real case: the user refines preferences (`Refine X: include …`, T-46/T-59 conversational refinement, or button reactions interpreted by T-17) but no NEW items have been collected since the last delivery. The recompute that the new preferences should drive doesn't fire — the user has to know to type `/rebuild_briefs` explicitly. | Add a second rebuild trigger: smart `/briefs` should also rebuild when `subject_preferences.version` (or the equivalent updated-at on `subject_sources` overrides) is newer than the last delivered digest's `sent_at`. Implementation: when `existing_items` is non-empty, query (a) `top_unsent_candidates` for fresh content as today, AND (b) the most recent `subject_preferences.updated_at` and `subject_sources.updated_at` for the subject; rebuild if either condition hits. Footer text differentiates: "1 new brief since 14:30" vs. "Preferences changed since 14:30 — re-ranked from current scores." | (1) Refining preferences then tapping `/briefs` (without new collection) rebuilds today's digest from current scores instead of resending stale composition. (2) Footer wording distinguishes "fresh content" from "preferences changed". (3) Both triggers can fire in the same call — combined message: "1 new brief + re-ranked from current scores." (4) Resend path is unchanged when neither trigger hits. | Closes the gap T-57 acceptance (3) implied but didn't quite enforce. Without it, refinement feels broken — user says "less hype" via Refine, taps Get Briefs, and gets the same hype back. |
| T-68 | done | T-48 follow-up — `_full_body` truncation in `TelegramDigestRenderer` is hardcoded at 1800 characters. Long-form content (podcast transcripts, in-depth Substack posts) gets cut off mid-paragraph regardless of subject. A user reading the AI/HR subject might want longer expansion; a user on the Ukraine war news subject might want a tighter cap. | Move the cap to a per-subject configuration: `subjects.brief_full_text_chars` (default 1800). `TelegramDigestRenderer._full_body` reads it from the subject passed into `render()`. Out-of-range values clamped (min 200, max ~4000 — Telegram's per-message limit is 4096 with overhead for header / footer / buttons). Wizard exposes the value as part of subject preferences once T-59's rich rule UI lands. | (1) Setting `subjects.brief_full_text_chars=3500` for a subject makes its full-view briefs use up to 3500 chars before truncation. (2) Default 1800 preserved for existing subjects. (3) Out-of-range values clamped; clamp logged at WARN. (4) Truncation still appends the `"\n\n..."` marker so the user can tell the body was cut. | Lets long-form-heavy subjects (podcasts, Substack) get longer expansions without bloating Telegram-only short-form subjects (X, news brief). Small ask, real UX win once T-11 segments arrive. |
| T-69 | P3 | T-57/T-58 follow-up — old `/get_digest` and `/rebuild_digest` Telegram commands are kept as deprecation aliases that print a "now /briefs" hint and run the new flow. They were intentionally retained for one release cycle, but no removal date is encoded anywhere — if we forget, they'll quietly accumulate as legacy. | Pick a removal milestone and write it down. Cheapest implementation: a single `DEPRECATED_COMMAND_SUNSET` constant in `telegram_service.py` set to a date (e.g. `2026-08-01`); the deprecation-hint message includes the date ("`/get_digest` is now `/briefs` and will be removed after 2026-08-01"). When the date passes, delete the alias handlers in a follow-up PR. Add a CHANGELOG note now. | (1) `DEPRECATED_COMMAND_SUNSET` exists with a documented date. (2) The deprecation message in the bot includes the date. (3) After the date, the aliases are removed and the bot replies "unknown command — try /briefs" if invoked. (4) CHANGELOG / README mention the sunset. | Avoids the typical "deprecated for one release cycle" drifting into permanent dual-implementation. Cheap to track, cheap to remove. |
| T-72 | done | `Stage Follows` for Apple Podcasts (and to a lesser extent LinkedIn) shows a wizard "Load Failed" alert even when the backend completes successfully. Confirmed live: ~80 Apple Podcasts subscriptions trigger ~80 sequential `https://itunes.apple.com/lookup?id=...` calls (~250ms each) for display-name resolution from T-43. Total wall time ~25–30 seconds — exceeds the wizard JS's `fetch()` default. Backend keeps running; the user sees a misleading error and the actual scrape completes. | (a) Stream progress via Server-Sent Events on `/api/stage-follows` so the wizard frontend renders a live status counter ("23 of 87 podcasts resolved…") instead of waiting for one big response. (b) Or: bound the iTunes lookup work — batch in groups of 10 with concurrency 4, OR cache lookups against a local SQLite table so a re-run is fast. (c) Bump the JS `fetch()` timeout to 120s as a stopgap until streaming lands. (d) When the backend is still running, suppress the alert and show "Working — check Logs tab for progress" instead. | (1) Stage Follows on a 87-podcast Apple library no longer triggers a Load Failed alert. (2) Wizard shows live progress (item count or percentage). (3) iTunes lookups are cached so a re-run finishes in <2s. (4) Existing pending-source rows back-fill on next stage-follows run. | Closes the most-noticeable wizard UX wart for power users with large podcast libraries. The backend already works; this is purely a frontend/observability problem. |
| T-70 | done | Wizard's `Save Runtime Settings` writes whatever's in the form to `.env` unconditionally — including blank values. Live regression: user reopened the wizard, the Telegram-token input was rendered empty (the wizard never re-reads the saved token for security), user clicked Save, and the previous `PCCA_TELEGRAM_BOT_TOKEN` line was overwritten with `PCCA_TELEGRAM_BOT_TOKEN=`. The agent restarted, read empty token, logged `Telegram service will be disabled`, bot went silent. No error surfaced to the user — only the `.pcca/logs/pcca.log` warning revealed the cause. | `DesktopCommandService.save_runtime_settings` should treat an empty token as **don't change** rather than overwrite. Concretely: `if not token.strip(): values.pop("PCCA_TELEGRAM_BOT_TOKEN", None)` so the existing `.env` line is preserved. Apply the same rule to any other secret-like field. As a backstop, the wizard form should warn before saving an empty token over an existing one ("Telegram token is empty — leave existing value? [Yes / Replace with empty]"). And on agent start, when the token is missing/empty, surface a visible error in the wizard (not just in the log file) so the user sees it without tailing logs. | (1) Saving with an empty token field preserves the existing `.env` value. (2) Saving with a non-empty token overwrites as today. (3) Wizard surfaces "Telegram service is disabled — token is missing" prominently when the agent reports `PCCA_TELEGRAM_BOT_TOKEN is not set`. (4) Test covers empty-token save preserving the previous line. | Closes a silent-data-loss footgun. The user lost their bot connection entirely without any visible indicator until they read the log — exactly the class of failure architecture.md §4.13 observability is supposed to prevent. |
| T-73 | done | The desktop Wizard is visually bulky and operationally confusing. It presents the first-run flow as one long form, mixes daily-use actions with source import/config/debug, and exposes backend implementation details such as separate session capture and rebuild buttons. The Subject step still asks for `Include terms` / `Exclude terms` / `High-quality examples` as structured form fields, contradicting Scenario 2 (free-form description → bot rephrases → user confirms via the T-46 conversational extraction). This makes real Scenario 1 testing feel less like a product and more like a maintenance console. **Live regression (2026-04-29 brief diagnostic):** subject `AI impact over IT jobs market` was created with empty `include_rules_json` / `exclude_rules_json` and produced 100% irrelevant Briefs (top items: Arizona electric utilities, Tim Berners-Lee, Morgan Housel finance). The curator applies a uniform ~0.6 score to every item when subject rules are empty, so the entire top-N is noise. Two defects converged: (i) `telegram_service.py:396` has an unconditional fallback that creates a subject with no preferences whenever `preference_extractor` is not wired; (ii) even when extraction runs, the conversational loop accepts the first thin input (`Create subject: <name>`) as a complete creation rather than asking follow-ups and looping until rules are non-empty. The wizard inherits the same gap if its add-subject textarea accepts a one-liner and persists immediately. T-73 must close both surfaces under one principle: **no path creates a subject without non-empty preference rules.** | Redesign the Wizard into a stylish, techy, compact, minimal UI with tab navigation: **Use**, **Sources**, **Config**, **Debug**. **Use**: start/stop agent, subject list, `Get Brief` action next to each subject, and add-subject flow. Add-subject is a **single free-form textarea** that calls the same `PreferenceExtractionService` (T-46/T-59) Telegram uses — wizard renders the proposed title + rephrased rules in plain English, the user confirms / refines / cancels in place. Drafts persist in `pending_subject_drafts` so a draft started in the wizard can be confirmed in Telegram and vice versa. Clicking a subject row drills into per-subject detail: preferences in plain English, per-subject suspended sources, current Brief route, with inline `Refine` (reopens the extraction loop with current description as context) and `Reassign Route` actions. Remove explicit `Rebuild Briefs`; `Get Brief` remains the single smart action and rebuilds automatically when new content appeared since last Brief generation, coordinating with T-57/T-67. **Sources**: `Get Sources` per platform; it checks session health and requests/session-captures only when needed, so there is no separate `Capture Session` button. Show a filterable source list by platform/status, and expose `Get Content` to collect fresh content and save new items. **Config**: Telegram token, timezone, Brief time, and other user-facing settings. **Debug**: state, logs, route table, source/session health, and any failure details. **First-run empty state**: when no subjects exist, Use shows a single CTA "Describe your first subject"; if the Telegram bot token is missing, Use redirects the user to Config first with a one-line explanation. **Default landing tab**: Use if onboarding is complete (token + at least one subject), Config otherwise. **Explicit deletions**: today's structured Subject form (`Include terms`, `Exclude terms`, `High-quality examples` inputs in step 5) is removed; today's separate `Capture Session` and `Rebuild Briefs` buttons are removed. **One conversational path, non-empty-rules invariant**: no shortcut anywhere creates a subject without going through `PreferenceExtractionService` and producing non-empty rules. (a) Remove the `telegram_service.py:396` fallback — if the extractor is not wired, fail loudly ("subject creation requires preference extraction; configure Ollama or restart the agent") instead of silently creating an empty-rules subject. (b) Subjects with empty `include_rules_json` AND empty `exclude_rules_json` cannot be persisted to the `subjects` table — enforce in `SubjectService.create_subject` (or a thin wrapping service) so wizard, Telegram, and CLI all share the invariant. Drafts live in `pending_subject_drafts` until rules land, then graduate. (c) The conversational loop actually loops: when extraction returns empty rules from a thin input, the bot/wizard responds "tell me more — what topics to include, what to exclude, any example articles", collects the next user turn(s), re-extracts on the merged description, and finalizes only when rules are non-empty. (d) The wizard's Save/Confirm button is disabled until rules are non-empty; same constraint as Telegram. | (1) Wizard has exactly four primary tabs: Use, Sources, Config, Debug. (2) Use tab shows agent start/stop, subjects, add subject (single free-form textarea, no include/exclude/examples form fields), and per-subject `Get Brief`; no rebuild button is visible. (3) Add-subject calls `PreferenceExtractionService` and shows the proposed title + rephrased rules; user can refine in place by sending corrections, or save / cancel. Drafts created here are visible to the Telegram path and vice versa. (4) Clicking a subject row opens per-subject detail with preferences (plain English), per-subject suspended sources, route, and inline Refine + Reassign Route actions. (5) Sources tab has `Get Sources`, filterable source list, and `Get Content`; session capture is invoked only as an inline prerequisite/repair flow when needed. (6) Config tab preserves existing token/timezone/Brief-time behavior and does not expose debug noise. (7) Debug tab contains current state/logs plus actionable failure details. (8) First-run empty state directs the user to Config when token is missing, then to Use's Add Subject CTA. (9) UI is compact and responsive on laptop widths; visual direction is intentional, minimal, and techy rather than default form cards. (10) `Include terms` / `Exclude terms` / `High-quality examples` form inputs no longer appear anywhere in the Wizard. (11) Existing backend actions remain covered by tests or smoke checks after the UI reorganization. (12) `telegram_service.py:396` unconditional fallback is removed; Telegram subject creation unconditionally invokes `PreferenceExtractionService`, and a missing/unwired extractor raises a visible error rather than silently creating an empty subject. (13) `SubjectService.create_subject` (or equivalent) rejects subjects whose include AND exclude rules are both empty; rejection is enforced at the service layer so wizard, Telegram, and CLI cannot bypass. (14) Conversational loop: when extraction returns empty rules, the next bot/wizard turn requests more detail and merges with prior turns until rules are non-empty or the user cancels. Test: starting from `Create subject: <name>` with no description must take at least one follow-up turn before a `subjects` row is created. (15) Regression test: subjects with empty rules cannot be persisted via any path (Telegram, wizard, CLI). (16) Live verification: recreating `AI impact over IT jobs market` through the new flow yields non-empty rules and topically relevant Briefs (no Arizona electric utilities, no Tim Berners-Lee biography, no Morgan Housel finance at top of list). | Turns the Wizard into a usable local product surface instead of a scaffolding page. Aligns the Subject creation surface with Scenario 2 + T-46 (single free-form description, conversational rephrase-and-validate) so the wizard and Telegram share one mental model. Reduces setup confusion, makes day-to-day usage obvious, and gives future UI work a sane information architecture. **Also closes the subject-irrelevance root cause documented in the 2026-04-29 brief diagnostic — without rules, the keyword scorer applies a uniform ~0.6 to every item; eliminating the empty-rules pathway is a structural fix that removes ~80% of irrelevance impact without touching the scorer.** |
| T-74 | done | LinkedIn collector returns 0 items across all 33 followed sources because the URL builder double-prepends `in/`. Confirmed live (2026-04-29 nightly run, run_id=24): orchestrator logged `Collecting source ... source_id=108 platform=linkedin identifier=in/ACoAAA2WrzMBW0tblYjqmElLdB695E8tu_ZWxqg`, and `src/pcca/collectors/linkedin_collector.py:26` constructs `f"https://www.linkedin.com/in/{source}/recent-activity/all/"` — yielding `linkedin.com/in/in/ACoAAA…/recent-activity/all/`, a hard 404. The `Stage Follows` flow uses the same captured cookies and works correctly, ruling out session expiry. Two tangled defects: (i) double `in/` prefix on every URL; (ii) follow-import stores the LinkedIn opaque URN (`ACoAAA…`) rather than a vanity slug, and LinkedIn's `/recent-activity/all/` page does not render reliably for URN-shaped paths even when the URL is otherwise well-formed. | (a) Make the URL builder idempotent over the prefix: strip a leading `in/` or `https://www.linkedin.com/` from `source` before composition. (b) At follow-import time, resolve URN → vanity slug by following the redirect from `linkedin.com/in/<URN>` to `linkedin.com/in/<vanity>` and store the vanity form in `sources.identifier`. (c) Backfill: a one-shot maintenance step that re-resolves URN-shaped LinkedIn rows and updates `sources.identifier`. (d) Unit test on the URL builder covering `in/<urn>`, `<urn>`, `https://www.linkedin.com/in/<vanity>`, and `company/<slug>` inputs. | (1) Re-running nightly collection on the existing 33 LinkedIn sources yields non-zero items for the majority (some accounts may genuinely have empty/private activity). (2) URL builder unit test passes for all four identifier shapes. (3) New follow-import stores vanity slugs for LinkedIn. (4) Backfill mechanism re-resolves previously-stored URN rows. (5) Assertion test: no `linkedin.com/in/in/` URL ever leaves the collector. (6) Live verification log shows scrape navigating to `linkedin.com/in/<vanity>/recent-activity/all/`. | Recovers the entire LinkedIn pipeline (currently 0/33 sources collecting). Single biggest content-volume defect — without this, no LinkedIn-derived Brief is possible. |
| T-75 | done | YouTube collector returns 0 items across all 7 followed sources (`@AndrejKarpathy`, `@LennysPodcast`, `UC-9-kyTW8ZkZNDHQJ6FgpwQ`, etc.). URL construction is correct (verified: `youtube.com/@AndrejKarpathy/videos`); the page loads. The failure is in `page.evaluate(...)` — selectors `a#video-title-link, a#video-title` find nothing because (i) `wait_until="domcontentloaded"` + 2.5s `wait_for_timeout` fires before YouTube's React renderer hydrates the video grid, and (ii) the selector set may not cover all DOM variants YouTube serves to headless contexts. The 405/403 errors in the log are noise on `generate_204` and `videoplayback?…` (autoplay attempts), not the navigation. YouTube channel `Videos` pages are public — no cookies/session required, so this is unrelated to T-37C session capture. | (a) Replace fixed timeout with `page.wait_for_selector("a#video-title-link, a#video-title", timeout=15000)` so the evaluate runs only after the grid mounts. (b) Add a fallback that parses `ytInitialData` from the inline `<script>var ytInitialData = {...};</script>` when DOM selectors return empty — this is the format yt-dlp uses and is far more stable than DOM ID selectors. (c) Detect interstitials (consent wall, age gate, "before you continue") via `<title>` and a small set of body text markers, and surface as a distinct structured log event (`consent_wall=true`) rather than silent `items=0`. (d) When both DOM and `ytInitialData` paths return empty AND no interstitial is detected, save a debug snapshot regardless of T-76's general empty-result mechanism. | (1) Re-running nightly collection on the existing 7 YouTube sources returns items for at least 5 (allowing for genuinely empty channels). (2) Both DOM-selector and `ytInitialData` paths have unit tests against fixture HTML. (3) Empty-result snapshot fires when both paths return zero. (4) Interstitial detection surfaces as a distinct structured log event, not a silent `items=0`. | Recovers YouTube as a content source. Same evidence shape as LinkedIn — the original audit speculated session, the real cause is local. Without this, no YouTube-derived Brief is possible. |
| T-76 | done | `BrowserSessionManager.capture_debug_snapshot()` is wired only into the `except Exception` branches of each collector, so a scrape that returns `[]` (page loaded, evaluate found nothing) leaves zero forensic trail — no screenshot, no DOM dump, no JSON breadcrumb. The 2026-04-29 audit found `.pcca/debug/browser/` completely empty despite ~40 sources returning `items=0`. The pipeline logs `INFO ... items=0` and moves on. This is the exact failure mode where artifacts matter most: 'page loaded fine, returned empty' is indistinguishable in logs from selector-mismatch / consent-wall / silent-redirect / SPA-not-yet-hydrated, and we cannot disambiguate without re-running the scrape with manual instrumentation. The current incident wasted hours of speculation that artifact capture would have answered in seconds. | Emit a debug snapshot when a collector returns 0 items, sampled to bound disk usage on legitimately-empty channels. (a) Add `BrowserSessionManager.capture_empty_result_snapshot(page, platform, source_id, sample_rate)`. (b) Each browser-driven collector (X, LinkedIn, YouTube) calls it after `page.evaluate(...)` returns empty, before `page.close()`. (c) Sample rate configurable via `PCCA_EMPTY_RESULT_SNAPSHOT_RATE` (default 0.33). (d) Snapshot file naming distinguishes empty-result from exception artifacts (`<platform>_empty_<ts>_<source_hash>.png` vs. `<platform>_failed_<ts>.png`) so `pcca debug-bundle` can include both with the same redaction. (e) Snapshot bundle includes rendered URL, `<title>`, `<meta name="title">`, and the first 4KB of HTML — enough to distinguish login-redirect / selector-mismatch / consent-wall at a glance without re-running. | (1) A collector returning `[]` produces an artifact under `.pcca/debug/browser/` with the empty-result name pattern. (2) Default sampling keeps a 30-source LinkedIn run to ~10 snapshots, not 30. (3) `pcca debug-bundle` includes empty-result snapshots with the same redaction as exception snapshots. (4) Existing exception-path snapshots are unchanged. (5) Snapshot includes title + first 4KB HTML so triage can read the page state without reproducing. | Closes the diagnostic blind spot exposed by this incident. Future "why is X returning 0?" is answerable from the artifact directory, not via source reading + speculation. |
| T-77 | done | When a platform-wide bug breaks every source on a platform, the nightly run continues marching through every source — 33 LinkedIn URLs in 2-second intervals, all hitting identical 404s, in the 2026-04-29 nightly run. That's exactly the bot-shaped traffic anti-abuse systems flag: uniform timing, uniform error response, no human-like backoff. A real human gives up after 2-3 dead links. Today there is no circuit breaker, so a single platform-wide defect (URL bug, selector break, anti-bot challenge, network instability) becomes N consecutive abuse signals against an account whose session we depend on for the rest of the project. T-07 (crawl throttling) covers timing variance; it does not cover "stop hitting this platform when it's clearly broken." | Add a per-platform circuit breaker to the pipeline orchestrator. Track consecutive failures per `(run_id, platform)`; once `consecutive_failures >= PCCA_PLATFORM_CIRCUIT_THRESHOLD` (default 5), abort the remaining sources for that platform in that run, log a structured event, and write `{"circuit_broken": ["<platform>"]}` to `run_logs.metadata_json`. Failure counts when: (a) collector raises any exception, (b) collector returns `[]`, or (c) collector raises `SessionChallengedError`. A successful (`items > 0`) source resets the counter mid-run. Breaker resets across runs — next nightly tries the platform again from scratch (no permanent disabling). Surface `circuit_broken` per-platform state in the wizard's Debug tab and in `pcca debug-bundle` summary. Telegram bot shows a one-time "LinkedIn paused — N sources skipped after consecutive failures" message at next digest delivery when the breaker tripped during the preceding run. | (1) Configurable `PCCA_PLATFORM_CIRCUIT_THRESHOLD` (default 5); test that 5 consecutive empty/exception results stops the platform's remaining sources in the same run. (2) `run_logs.metadata_json` records `{"circuit_broken": ["linkedin"]}` when the breaker tripped. (3) Counter resets across runs. (4) A single mid-streak success resets the counter inside the same run. (5) Per-platform isolation: one platform's breaker tripping does not abort another platform in the same run. (6) Wizard Debug tab and Telegram digest both surface the `circuit_broken` state with the affected platform name. (7) Unit tests cover threshold trip, mid-streak reset, and per-platform isolation. | Closes the abuse vector exposed by the LinkedIn URL bug. Today a single defective collector burns through every followed account in sequence, accumulating bot-shaped error signal against sessions that are expensive to re-establish. Bounds damage at threshold+1 sources, not all M. Gives clear observability when a platform is broken, instead of silent zero-volume runs. |
| T-78 | done | Telegram draft handler `_handle_pending_subject_draft` ate explicit-intent commands while a draft was pending. Confirmed live (2026-04-30 03:55:38 log + screenshot): user typed `List subjects`, intent parser correctly identified `action=list_subjects`, but the bot returned the empty draft template instead of a subject list. Root cause: bypass conditional at `telegram_service.py:647-653` only let `HELP`, `RUN_READ_CONTENT`, `RUN_GET_DIGEST`, `RUN_REBUILD_DIGEST` through to the main router; every other intent (`LIST_SUBJECTS`, `LIST_SOURCES`, `ADD_SOURCE`, `REFINE_PREFERENCES`, `CREATE_SUBJECT` with name, etc.) was fed into the draft handler as a refinement, where the heuristic extractor produced empty rules and the bot replied with the same "Tell me more" template. Effectively any pending draft locked the user out of every other Telegram command until they explicitly typed `cancel subject`. | Invert the bypass logic: only treat the message as a draft refinement when the parsed `intent.action is IntentAction.UNKNOWN` (and the user did not type a slash-command). All explicit intents fall through to the downstream router by default. The `cancel`/`save` literal checks above the bypass remain unchanged so the user can still finalize/abandon a draft. | (1) `List subjects` while a draft is pending lists subjects rather than returning the draft template. (2) `Add source x:foo to Vibe` while a draft is pending invokes the source-add handler. (3) `Help` continues to bypass as before. (4) `cancel subject` and `save subject` literals still finalize the draft regardless of intent classification. (5) A truly unknown utterance (free-form correction) is still routed to the draft handler. | Removes a hard UX trap: any pending draft from earlier sessions silently swallowed every subsequent command until cancel. Especially painful with the legacy empty-rules drafts from before T-73, which the user couldn't escape without typing `cancel subject`. |
| T-79 | done | Heuristic preference extractor refused to fall back to description-based term extraction whenever any prior draft existed, even one with empty rules. Proven live (2026-04-30 04:09:05 log run with user's 359-char description as input): `Heuristic description-fallback decision: explicit_include_empty=True previous_truthy=True fallback_taken=False` — the salvage path that would have pulled noun phrases from the user's description was skipped because `previous` was a non-None `SubjectDraft` object, even though `previous.include_terms == []` and `previous.exclude_terms == []`. Net effect: once any draft existed, the heuristic could only ever produce empty rules from a non-explicit description, locking the user into the "Tell me more" loop with no way to make extraction progress. | Replace the `not previous` guard with `not previous_has_rules` where `previous_has_rules = bool(previous and (previous.include_terms or previous.exclude_terms))`. The fallback now fires whenever the user gave no explicit `include:`/`exclude:` markers AND the prior draft has no usable rules. If the prior draft already has terms, leave them in place — the user is refining, not starting over. | (1) Heuristic call with an empty-rules `previous` and a rich English description extracts non-empty terms via the description fallback. (2) Heuristic call with a rules-bearing `previous` and a thin user message preserves the prior rules (no regression). (3) Logging shows `fallback_taken=True` for the empty-previous case. | Was the smoking-gun bug that produced infinite "Tell me more" loops. Fixed in tandem with the LLM-first switch (T-81): the LLM is the primary path; this fix means the heuristic-fallback case also recovers instead of dead-locking. |
| T-80 | P2 | Heuristic preference extractor only matches literal keywords (`include:`, `focus on`, `interested in`, `exclude:`, `avoid:`, `not interested in`, `no `, `drop`, `remove`, `less`, `practical`/`concrete`/`actionable`/`high-quality`/`good looks like`). Proven live (2026-04-30 04:09:05 log on user's 359-char natural-English description): `Heuristic explicit-keyword pass: explicit_include=0 explicit_exclude=0`. The user's request — *"i would like to be aware of thoughts of SMEs… if thought didn't contain any novelty, insight, reasoning it could be better to skip it"* — has none of those literal markers, so the explicit-keyword pass found nothing. With Ollama as the primary path (T-81) this no longer dead-locks the user, but the heuristic fallback when Ollama is unreachable is still useless for natural English. | Broaden `_terms_after_keyword` and `_terms_from_description` to handle natural-English patterns: noun-phrase extraction over the user's description (filtered against STOPWORDS); soft-exclude markers (`skip`, `not interested`, `didn't contain`, `better to avoid`, `no need for`); soft-quality markers (`I want`, `I'd like`, `I care about`, `with reputation`). Optionally use spaCy or a tiny POS tagger if dependencies allow; otherwise hand-rolled regex on noun-like patterns. The goal is "if Ollama is offline, the heuristic should still produce *some* meaningful terms from a 50+ word description." | (1) Re-running the user's exact 359-char message through the heuristic with `previous=None` produces ≥3 include terms and ≥1 exclude term. (2) Stopwords-only inputs ("hello hi how are you") still produce empty rules. (3) Existing tests for explicit-keyword extraction continue to pass. (4) No new heavyweight dependency. | Restores the heuristic as a usable fallback for offline / Ollama-down operation. Without this, the heuristic-only path remains dead for natural-language input — fine when Ollama is healthy, but a regression risk if Ollama becomes unreachable mid-conversation. |
| T-81 | done | When `PCCA_OLLAMA_ENABLED=true` but the Ollama server is unreachable (not running, wrong URL, timed out), `ModelRouter.extract_subject_preferences` logs a WARNING and returns `None`. `PreferenceExtractionService` then silently falls back to the heuristic. The user sees no UI cue that the LLM path was attempted and failed — the wizard / Telegram reply is identical to "you typed something the heuristic couldn't parse." User flipped the flag expecting better behavior, got the same loop, and had no way to know the agent silently degraded. The 25-second `httpx` timeout in `extract_subject_preferences` was also too tight for cold-start: the first call after Ollama starts spends 30-60s loading the model into VRAM, which manifests as a connection-timeout that looks identical to "Ollama not running." | (a) Add an `extraction_warning: str | None` field to `ExtractedSubjectDraft`. Set it from `PreferenceExtractionService.extract` when `model_router.enabled is True` AND the LLM result is None — i.e. the LLM was attempted but failed. (b) Both renderers (`telegram_service._format_subject_draft` and `desktop_command_service.draft_subject` message string) append the warning to the user-facing reply with a `⚠️` prefix so it stands out without obscuring the draft. (c) Bump `httpx.AsyncClient` timeout in `extract_subject_preferences` from 25s to 90s — subject creation is rare enough that cold-start latency dominates over hot-path latency. | (1) Setting `PCCA_OLLAMA_ENABLED=true` while Ollama is not running surfaces `⚠️ LLM extraction (Ollama) was unreachable or timed out; using a basic keyword fallback. Start Ollama (brew services start ollama) for better results.` in both Telegram and the wizard. (2) When Ollama is healthy and returns a result, no warning is shown. (3) Cold-start LLM call (first call after `ollama serve` starts) succeeds within 90s instead of timing out at 25s. (4) Disabling Ollama (`PCCA_OLLAMA_ENABLED=false`) does not surface the warning — only the attempted-and-failed path does. | Closes the silent-degradation footgun: user flips a flag, expects different behavior, gets the same broken loop, has no way to know why. The cold-start timeout bump prevents a related class of "Ollama is running but the first call timed out" confusion. Together they make Ollama-as-default safe to test without watching `pcca.log` to see what happened. |
| T-82 | done | Wizard's `Get Content` button (Sources tab) is visually grouped with the per-platform `Get Sources` toolbar (the `Platform` dropdown sits directly above it), implying platform-scoped behavior. In practice, `desktop_command_service.read_content` calls `pipeline_orchestrator.run_nightly_collection()` which iterates **all** platforms regardless of dropdown selection. Confirmed live (2026-04-30 user test, log lines 2174 + 2224): user clicked Get Content with platform=X, then again with platform=youtube; both clicks ran full nightly runs (143 sources_seen, all platforms, ~25 min each), opening LinkedIn / Apple Podcasts / X / YouTube browser contexts in succession. User reported confusion: "based on Wizard UI Read Content is launched per platform, no?" | (a) Add an optional `platform` parameter to `read_content` service method and `/api/content/read` endpoint; when provided, restrict the orchestrator's iteration to sources where `sources.platform == platform`. (b) Wizard JS reads `byId('platform').value` and passes it. (c) When platform is None, run all platforms (legacy behavior, used by scheduled nightly). (d) Button label updates to "Get Content (X)" / "Get Content (YouTube)" based on dropdown selection so the scoping is visually unambiguous. (e) Toolbar hint copy: "Get Content runs the collector for the selected platform only. To run everything, use Run Now in Debug." | (1) Clicking Get Content with platform=youtube only crawls YouTube sources; no LinkedIn / X / Apple Podcasts contexts are launched. (2) Default scheduler nightly still runs all platforms (no regression). (3) Button label reflects the current dropdown value. (4) Test covers both scoped and unscoped invocations of `read_content`. | Closes the misleading-affordance bug that wasted 50 minutes of the user's time on the 2026-04-30 test. Aligns the Sources tab with the user's mental model: "the dropdown scopes everything in this card." |
| T-83 | done | Long-running Wizard actions (`Get Content`, `Stage Follows`, full-nightly equivalents) have no idempotency guard: clicking the button twice fires two backend runs concurrently. Confirmed live (2026-04-30): user clicked Get Content twice in ~5 minutes; both fired `run_nightly_collection()` and ran in parallel for ~22-25 min each, sharing browser contexts and producing overlapping log noise. Of the second run's 1,493 collected items, only 1 was new (everything else was already inserted by the first run). Net waste: ~22 minutes of crawling, plus user confusion ("nothing was happening for 5 mins", "browser started to open" in surprising order). Compounding issue: the wizard JS `postAction('/api/content/read')` does not pass a `timeoutMs`, so its `fetch()` hits a default timeout long before the 25-min backend run finishes — yielding the misleading "Load Failed" alert (same UX shape as the closed T-72 was for Stage Follows; the fix didn't carry over to content/read). | (a) **Backend lock**: maintain `desktop_command_service._inflight_actions: dict[str, asyncio.Task]` keyed by action name (`"read_content"`, `"stage_follows"`, etc.). On invocation, if an in-flight task exists, return `CommandResult(False, "A {action} run is already in progress; check Logs.", {"already_running": True})` instead of starting a second. The first-run task removes itself from the dict on completion. (b) **Wizard UX**: button is disabled while in-flight; `loadState` polls show a "Running: Get Content (started 03:12)" notice in the Sources tab; clicking again surfaces the polite no-op message. (c) **Fetch timeout**: long-running endpoints (`/api/content/read`, `/api/stage-follows`, `/api/briefs`) get an explicit `timeoutMs: 1800000` (30 min) in the wizard so the JS doesn't bail with "Load Failed" while the backend is still working. When the backend genuinely fails, the user sees the real error; when the backend is slow, no false alert. (d) **Per-platform scoping (T-82)** is independent but reduces the surface of this issue, since users won't double-click as often once the dropdown actually scopes. | (1) Two concurrent invocations of `read_content` cannot run; the second returns the polite no-op result and the wizard surfaces "already running". (2) Wizard `Get Content` button shows in-flight state across page refreshes (state derived from `_inflight_actions`, not local JS). (3) `Get Content` on a 25-min nightly run does not produce a "Load Failed" alert in the wizard — neither during nor after, regardless of duration. (4) When the backend genuinely errors, the wizard shows the real error (regression test). (5) Same idempotency / timeout shape applied to `stage_follows` and `get_briefs` for consistency. | Closes two compounding UX bugs from the 2026-04-30 test: misleading error during long actions, and silent doubling of work via accidental double-click. Without it, every long action remains a coin-flip between "did it work" and "did I just kick off two of them." |
| T-84 | done | Bridge between today's flat-schema preference extraction (T-46, closed) and the rich-rule schema (T-59, P1, open). Without a stopgap, the months-or-so before T-59 lands continue to silently drop authority/conditional/anti-engagement nuances from user descriptions (proven 2026-04-30 — see T-59 evidence). **Live regression (2026-04-30 brief diagnostic):** the LLM extraction also drifts the other way — for subjects described primarily by quality criteria, the LLM puts the **quality criteria** in `include_terms` and **drops the topic** entirely. Concrete examples from current DB: subject `Ukraine War News` has `include_rules.topics=['reputable sources', 'high quality analytics', 'key thoughts', 'facts']` — zero topic anchors (no `ukraine`, `russia`, `kyiv`, `war`, `donbas`). Subject `AI impact over IT jobs market` has `include_rules.topics=['smes', 'podcast thoughts', 'video interviews', 'novelty', 'insight']` — also zero topic anchors. Result: Pass-1 keyword scorer cannot match abstract quality criteria against actual content text (those phrases never literally appear in tweets/podcasts), Pass-1 collapses to uniform ~0.6 baseline for every item, and the top-100 fed to Pass-2 LLM rerank becomes random noise (1,425/1,634 = 87% Apple Podcasts by sheer volume). Pass-2 then "reranks random noise" and the resulting Briefs are dominated by off-topic Apple Podcasts: `Ukraine War News` top-5 = Claude Code release notes / Staph Retreat / Microwave Weapons / Master Your Focus / The NFL — none about Ukraine. `AI impact over IT jobs market` top-5 = Tim Berners-Lee biography / Morgan Housel personal finance / baseball / Iran war geopolitics — none about AI or jobs. Pass-2 rerank deltas confirm the LLM is correctly identifying off-topic content (123 of 180 deltas at max-negative -0.25) but the post-Pass-2 score range (~0.35 floor, ~0.75 ceiling) cannot recover when the pre-rerank shortlist is random. **Pass-1 must have real topic keywords to surface relevant candidates; no amount of Pass-2 cleverness fixes a noise-shortlist.** | Update `model_router.extract_subject_preferences` prompt to explicitly tell the LLM: **(a) Topic vs. quality separation** — `include_terms` and `exclude_terms` must be **literal topic words that would appear in the content** (proper nouns, named entities, domain terms). Quality / authority / source / conditional / anti-engagement criteria go into `quality_notes`. Examples in the prompt: *user says "Ukraine war news from reputable sources" → include_terms=["ukraine", "war", "russia", "kyiv"], quality_notes="prefer reputable sources, avoid propaganda"*. (b) **Topic enrichment** — when the user describes a topic abstractly ("AI impact on IT jobs"), the LLM expands it into 5-10 likely-to-appear-in-content terms (`["ai", "automation", "ml", "jobs", "employment", "layoff", "displacement", "augmentation"]`). (c) **Authority/source listing** — if the user names companies, domains, or individuals as authoritative sources, list them as a comma-prefixed line in `quality_notes`. (d) **Conditional rules** — summarize in `quality_notes`. (e) **Anti-engagement** — explicit "Engagement: do not boost" line. (f) **Title** — up to 7 words; preserve distinctive descriptors. `quality_notes` becomes a kitchen-sink — hacky but the user's intent stops being silently dropped, and the resulting strings prime T-59's eventual rich-rule extraction. (g) **One-shot fix for existing subjects**: a `pcca rebuild-subject-rules <subject_id>` CLI that re-runs extraction on the subject's stored description with the new prompt. Existing subjects with broken rules can be repaired without recreating them. | (1) Recreating `Ukraine War News` from the user's original description produces `include_terms` containing real Ukraine-related topic anchors (≥4 of: ukraine, russia, kyiv, war, donbas, putin, zelensky, frontline) and `quality_notes` containing the quality criteria. (2) Recreating `AI impact over IT jobs market` produces topic anchors (≥3 of: ai, jobs, automation, ml, employment, layoff, displacement) and `quality_notes` with quality criteria. (3) The 2026-04-30 description for `AI Tools & Tips` (quoted in T-59 problem) round-trips with authority list + conditional excludes + anti-engagement note in `quality_notes`. (4) `pcca rebuild-subject-rules` re-extracts an existing subject's rules in place (new `subject_preferences.version` row). (5) When the user input has no authority / conditional / anti-engagement signal, `quality_notes` stays short and natural (no template noise). (6) Existing tests still pass; the heuristic fallback path is unchanged. (7) After T-84 lands, recomputing today's Briefs for `Ukraine War News` puts at least one Ukraine-related item in the top-5 (the underlying content exists in the DB; the issue is that the rules can't surface it). | Stopgap that closes the worst of the user-visible "this is a wrong interpretation" frustration AND the "Briefs look totally irrelevant" frustration without waiting for T-59's full schema rebuild. Cheap to ship; cheap to remove once T-59 lands. **Promoted P2 → P1 because brief relevance is now actively broken in user-visible ways, not just theoretically suboptimal.** |
| T-85 | done | T-77 follow-up — circuit breaker counts `empty_result` the same as `404`/`session_challenge`/exception. **User feedback (2026-04-30):** *"a mix of private profiles, no recent activity should not lead to stopping the scraping process. the idea was to stop when we continue opening unexisting pages, which can lead to banning."* Direct evidence: in run_id=29 LinkedIn streak reset four times before tripping (15:20:11 / 15:20:27 / 15:20:43 / 15:20:59) — meaning some LinkedIn sources legitimately had content (after T-74's URN→vanity backfill resolved them) and several others legitimately returned empty (private profile, no recent activity). The breaker tripping in this case is wrong: nothing about empty-but-200-OK pages signals bot-shaped behavior to LinkedIn anti-abuse. Hard 404s and login-redirects DO signal it — those are the real bot-shaped patterns. | Differentiate failure types in `pipeline_orchestrator._record_platform_failure`: introduce two failure classes — `bot_shaped` (`reason='http_404'`, `'session_challenge'`, `'http_403_login_redirect'`, raised exception during navigation) and `empty_legitimate` (`reason='empty_result'` from a 200-OK page that rendered without obvious blockers). Only `bot_shaped` failures count toward the existing `PCCA_PLATFORM_CIRCUIT_THRESHOLD` (default 5). Empty results count toward a separate, much higher `PCCA_PLATFORM_EMPTY_THRESHOLD` (default 25, near the typical fleet size) so a user who follows mostly inactive accounts isn't punished, but a true platform-wide regression (every source returns empty) still trips eventually. Log lines and metadata distinguish: `circuit_broken_reason: ["bot_shaped"]` vs. `circuit_broken_reason: ["empty_legitimate"]`. The Wizard Debug surface and Telegram footer carry the reason. T-76 empty-result snapshots remain as they are — independent of the breaker count. | (1) Setting up a fixture where 5 LinkedIn sources legitimately return empty (200 OK, page renders, no posts) does NOT trip the breaker; 5 sources returning 404 / session_challenge DOES trip. (2) `run_logs.metadata_json.circuit_broken_reason` carries the failure class. (3) `PCCA_PLATFORM_EMPTY_THRESHOLD` configurable; default 25. (4) Existing test for breaker trip (5 consecutive failures) is updated to use `bot_shaped` reasons explicitly. (5) New test verifies empty-result sources don't trip the bot-shaped counter. | Closes the "user follows real accounts that don't always have content → breaker punishes them" misclassification. Restores the bot-defense intent (don't hammer 404s) without false positives on legitimately-quiet accounts. Without this, the breaker does the right thing for the wrong reason — it stops collection on the third LinkedIn quiet day too. |
| T-86 | done | T-75 follow-up — root cause now proven from T-76 debug snapshots, and the current fix path (DOM `wait_for_selector` + inline `ytInitialData` parse) cannot work because the data isn't on the page. All 10 youtube_empty_*.json snapshots from the 2026-04-30 run show `articles: 0`, `bodyTextLength: 1,943-4,056` (just navigation chrome + channel name), no video grid, no inline `ytInitialData` payload. YouTube serves a stripped page in headless contexts; the video listings are XHR-loaded after hydration that the headless browser doesn't trigger. T-75 attacked the wrong layer — neither selector waits nor ytInitialData regex parsing can extract data that isn't there. | Replace the browser-based videos-page scrape with **YouTube's public RSS feed** at `https://www.youtube.com/feeds/videos.xml?channel_id=UC...`. This is what yt-dlp falls back to: public, no auth, no anti-bot, returns the latest 15 videos with timestamps, titles, descriptions, view counts. (a) For `UC...` channel-ID sources, hit the RSS feed directly via `httpx`. (b) For `@`-handle sources, resolve handle → channel ID once at follow-import or on first scrape (one-shot scrape of the channel `/about` page or a tiny inline `<link rel="canonical">` extraction; cache the resolved ID in `sources.metadata_json` so this is one-time). (c) Drop the wait_for_selector + ytInitialData paths from the YouTube collector — they're dead code on real channels in headless mode. (d) Keep browser-based access only for transcript extraction (separate, post-T-86). (e) The empty-result T-76 snapshot path no longer fires for YouTube under normal operation (RSS path doesn't render a browser page). | (1) Re-running collection on the existing 7 YouTube sources via the RSS feed yields items for at least 6 (allowing for newly-empty channels). (2) `@`-handle channels resolve to UC IDs once, and the resolution is cached in `sources.metadata_json`. (3) Items have populated `published_at`, `title`, `author`, and `view_count` (the RSS feed includes views). (4) Browser context is no longer launched for YouTube during normal nightly. (5) Live regression: re-running the 2026-04-30 run shape on the same channels does NOT trigger `empty_result` traces or the T-77 circuit breaker. | Recovers YouTube as a content source. T-75 was the right diagnosis at the time but the solution turned out to be wrong because we didn't have the snapshot evidence yet — now we do. The RSS path is structurally simpler, has no headless-detection failure mode, and is what every reputable YouTube scraper uses as the primary path. P0 because it blocks Brief diversity (currently 87% Apple Podcasts) and the other "fix YouTube" lever has been spent. |
| T-87 | done | Keyword-based preference schema (`include_terms` / `exclude_terms` + free-text `quality_notes`) is a fundamentally lossy compression of user intent, and no amount of prompt engineering on the extraction LLM (T-46/T-59/T-84) closes the gap. **Direct evidence (2026-05-01 review):** Pass-1 scorer in `pipeline/curation.py:112-120` is *weighted-additive-OR-with-substring-match*: `include_hits = sum(1 for term in include_terms if term.lower() in text); relevance += min(0.35, include_hits * 0.12)`. There is no AND, no phrase boundary beyond substring, no proximity, no synonym handling, no negation, no semantic similarity. **Concrete subject-by-subject lossiness** from the current DB: (a) `AI Tools & Tips` — user said "from companies leading in AI domain, from their employees, from thought leaders" → encoded as topic-keyword `"leading ai companies"`. The matcher then searches content text for that literal string, *inverting* the user's intent: a Karpathy tweet "good thread" scores low (no match), while a spammer's "top tips from leading AI companies you must know" scores high (match). The user's anti-engagement signal ("even though high engagement, useless") has no schema home at all — the trust score still positively weights engagement (`trust += min(0.15, engagement_strength * 0.10)`), so the exact spam they described gets boosted. (b) `Ukraine War News` — user wanted Ukraine analytics → encoded as `["reputable sources", "high quality analytics", "key thoughts", "facts"]`. Zero topic anchors. The matcher searches content for "key thoughts" — never appears in any tweet/podcast. Every Ukraine-related and every off-topic item scores at the uniform 0.6 baseline; top-5 ends up dominated by Apple Podcasts noise (Claude Code release notes, Staph Retreat, Microwave Weapons, NFL). (c) `Ukrainian Sole Proprietorship Laws` works *only* because the user's description happened to fit keyword shape — demonstrating the schema works only when intent is already topic-words-shaped. When intent is about authority, conditional logic, anti-signals, or quality, the schema mangles it on every extraction round. T-46/T-59/T-84 are increasingly elaborate workarounds for a wrong primitive; even with a perfect LLM, you cannot encode "exclude unless author is in {…}" or "discount engagement-correlation" as flat keyword lists that an additive substring scorer can use. | **Replace the keyword-extraction pipeline with embedding-based semantic scoring + LLM long-context rerank.** No more compressing the user's natural language into structured rules. (a) **Schema**: subject gains `description_text` (the user's full English) + `description_embedding` (vector); items gain `content_embedding` (vector, computed once at ingest from `title + raw_text`). Existing `include_rules_json`/`exclude_rules_json` schema retained as a *compatibility layer* for one release, then deprecated in T-87b. (b) **Embedding model**: Ollama-hosted `bge-small-en-v1.5` (33MB, ~50ms/text) or `nomic-embed-text:v1.5` (270MB, multilingual). Local, fast, no external dependency. New `EmbeddingService` mirroring the existing `ModelRouter` shape. (c) **Pass-1 (replaces keyword scorer)**: cosine_similarity(`subject.embedding`, `item.embedding`) batched across all items per subject. Top 100 by similarity become the rerank candidate pool. Cheap (1500 items × dot-product), batch-friendly, handles synonyms / paraphrase / multilingual / abstract topics natively, no extraction needed. (d) **Pass-2 (smarter, fewer calls)**: send the LLM the user's **full description** plus 20 top candidates in a single long-context call ("Score each 0-1 with reasoning, considering authority, conditional rules, and anti-signals stated in the description"). Returns ranked top-5 with structured rationale. One LLM call per subject per nightly, not per-item — cheaper than the current per-item rerank capped at 100. (e) **Refinement loop**: button reactions (👎 / 🚫) and free-text replies ("less hype like this") append a sentence to `description_text`, re-embed. The subject *evolves* through use; no rules editor, no extraction loop, no "Tell me more before I save it." (f) **Structured rules layer (T-59 supersession)**: T-59's authors/sources lists become an explicit-override layer **above** the embedding scorer (hard whitelists / blacklists, e.g. "never show me posts from @yourdailyAI") rather than the primary mechanism. T-59 is repointed accordingly. (g) **Migration**: schema additions are non-destructive; new scorer runs in shadow alongside keyword scorer for a release; A/B compare top-5 per subject in run_logs; deprecate keyword path once embedding consistently wins. | (1) `EmbeddingService` wraps Ollama embeddings endpoint; unit-tested against fixture text. (2) `subjects.description_embedding` and `items.content_embedding` columns exist (migration); existing rows back-fill on next pipeline run. (3) `pipeline/curation.py` Pass-1 has an embedding scorer alternative selected via `PCCA_SCORER=embedding|keyword|both` (default `both` during shadow period). (4) Pass-2 long-context call wires `subject.description_text` directly as the system prompt input plus a list of top-20 candidates' `title + first 1000 chars`; returns top-5 with rationale. (5) Refinement: button-react and free-text reply both append to `description_text` and trigger re-embed (one new `subject_preferences` row, version+1, append-only invariant preserved). (6) Live regression: recreating `Ukraine War News` from the user's original description and running the embedding pipeline yields ≥3 actually-Ukraine items in top-5 (vs. 0 today). (7) Live regression: recreating `AI Tools & Tips` yields ≥2 items from the user's authority list (Anthropic / Karpathy / OpenAI / similar) in top-5 and 0 spammy listicles. (8) Shadow A/B run_logs include both scorer outputs for the same items so the user can compare side by side. (9) After deprecation milestone (T-87b), `include_rules_json` / `exclude_rules_json` are no longer read by Pass-1; T-59's authors/sources are read as a separate hard-filter layer. (10) Wizard / Telegram subject-creation flow simplifies to a textarea — no extraction round-trip — once shadow validation passes. | Closes the entire class of "extraction interpretation is wrong" / "Briefs are irrelevant" / "schema can't express my real rule" frustrations documented across T-46, T-59, T-79, T-80, T-84, and the 2026-05-01 review. The keyword schema was a cheap-matcher proxy from before the codebase had Ollama wired in; it's no longer the right primitive. Embedding-first scoring uses the user's intent directly, handles abstract topics / synonyms / multilingual content natively, eliminates extraction lossiness, and reduces total LLM calls per nightly run. The user-facing surface simplifies to "type what you want, get Briefs"; the system internals get *less* code, not more. **Single largest quality lever in the backlog — supersedes T-46/T-59/T-84 as the primary scoring mechanism, while T-59 retains a smaller role as the explicit-override layer.** |
| T-88 | done | T-83's idempotency guard protected Wizard/manual duplicate clicks but did not protect collection at the orchestrator boundary. APScheduler `max_instances=1` prevents scheduler-vs-scheduler overlap, but a manual Wizard/CLI run during a scheduled nightly could still enter `PipelineOrchestrator.run_nightly_collection()` concurrently and share browser/session resources. | Add a DB-backed `runtime_locks` table and acquire a global `nightly_collection` lock inside `PipelineOrchestrator.run_nightly_collection()` before starting the run log. If the lock is held, return a structured `skipped_already_running` result instead of crawling. Release the lock in `finally`, and expire stale locks after a conservative TTL so a crashed process does not block forever. Wizard `Get Content` converts the skipped result into the same polite already-running no-op UX as T-83. | (1) Two overlapping calls to `run_nightly_collection()` cannot both crawl. (2) Scheduler, Wizard, CLI, and separate app instances share the same DB lock. (3) Manual `Get Content` during an existing collection returns a polite already-running message. (4) The lock is released after success/failure and stale locks expire. | Closes the remaining collection re-entrancy gap from T-83 and reduces platform-risk noise from overlapping browser crawls. |
| T-89 | done | T-84 shipped a correct `pcca rebuild-subject-rules <id>` repair path, but it was CLI-only. That makes the fix hard to discover during the Wizard-first dogfood flow, especially for subjects created before the improved extraction prompt that still have bad topic anchors. | Add a Wizard subject-detail `Rebuild Rules` action and `/api/subjects/rebuild-rules` endpoint. The action calls the existing `DesktopCommandService.rebuild_subject_rules()` using the stored subject description/current rules, creates a new preference version, refreshes the subject detail, and surfaces success/failure inline. | (1) The subject detail panel exposes `Rebuild Rules`. (2) Clicking it calls the new endpoint and writes a new `subject_preferences` version. (3) The Wizard refreshes include/exclude terms after rebuild. (4) CLI rebuild remains available. | Makes the T-84 repair path part of the normal user journey instead of hidden operator knowledge. |
| T-90 | done | T-87 made embeddings lazy, so the first interactive Brief/scoring run on an existing DB can spend minutes embedding every historical item with little visible progress. That feels like a hang even if it eventually finishes. | Add an explicit embedding backfill path that warms missing subject/item embeddings in batches with bounded concurrency, reports progress, and optionally rescores existing items after the cache is warm. Expose it as `pcca embed-backfill` and a Wizard Debug action. | (1) `pcca embed-backfill` warms missing embeddings and prints subject/item progress. (2) Wizard Debug exposes `Backfill Embeddings`. (3) Backfill uses bounded concurrency and 50-item batches. (4) Re-running after a successful backfill skips already-current embeddings. (5) Optional rescore rebuilds existing `item_scores` from the warmed cache. | Moves the slow first-run hit out of the user's "Get Brief" click and gives us an obvious repair action when embedding scoring is enabled. |
| T-91 | done | If Ollama's embedding endpoint is unavailable, T-87 silently falls back to keyword scoring aside from counters buried in logs. The user can get weaker Briefs without knowing the quality lane degraded. | Detect high embedding fallback rates per subject, log a warning, persist degradation details in `run_logs.metadata_json`, and surface the latest degradation in Wizard Debug/Failures with an actionable hint to check Ollama/backfill. | (1) A run where subject/item embeddings fail records `embedding_degraded=true` and per-subject fallback details in run metadata. (2) The Wizard failure box shows an embedding degraded warning. (3) Normal healthy embedding runs do not show the warning. | Prevents invisible quality degradation and makes local Ollama problems debuggable during dogfood. |
| T-92 | done | Subject embeddings included legacy include/exclude terms even when a full free-form description existed. For old subjects, those terms often represent quality labels like "reputable sources" rather than topic intent, pulling the embedding away from the user's real subject. | Build subject embeddings primarily from `subjects.description_text` plus optional quality notes; only fall back to include/exclude terms when no free-form description exists. Add text-hash cache keys so old bloated embeddings are treated as stale and recomputed. | (1) Subject embedding text with a description does not include `Include:` or `Avoid:` sections. (2) Old embeddings without the new text hash are invalidated naturally on next score/backfill. (3) Legacy subjects without descriptions still have a fallback embedding text. | Keeps embedding similarity aligned with the user's actual natural-language intent instead of keyword-extraction leftovers. |
| T-93 | P2 | T-87's A/B observability stores keyword shadow scores only inside rationale strings, which makes later analytics brittle. | Persist structured shadow-score metadata for embedding-vs-keyword comparisons, e.g. `shadow_scores: {keyword, embedding}` per scored item or in a queryable run artifact. Keep rationale strings human-readable but not the only machine-readable source. | (1) A `PCCA_SCORER=both` run stores structured keyword and embedding scores. (2) A test can read the comparison without parsing `rationale_json.reason`. (3) Existing Brief rendering still works. | Makes the shadow period measurable instead of relying on string parsing. |
| T-94 | P3 | Wizard `Rebuild Rules` immediately writes a new preference version. The prior version is preserved, but there is no confirmation, so a user can accidentally make a rebuild the active version. | Add a small confirmation modal before rebuilding rules from the Wizard. Explain that the previous version remains in history but the rebuilt version becomes active. | (1) Clicking `Rebuild Rules` asks for confirmation. (2) Cancel performs no write. (3) Confirm preserves the existing endpoint behavior. | Small safety polish for a potentially surprising repair action. |
| T-95 | done | Embedding-rollout migration UX has two unguarded edges that bite a first-time user. (a) **Cold-cache hang**: `PCCA_SCORER` defaults to `"both"` (embedding + keyword shadow). T-90 backfill is opt-in (CLI or wizard Debug action). A user who installs, runs the agent, and clicks `Get Brief` without first running backfill triggers lazy per-item embedding inside the scoring loop; on a 1,634-item DB with Ollama at ~50ms-1s per embed, the first scoring run hangs for 5-30 minutes with no progress indicator. T-83's 30-min wizard timeout *probably* covers it but the UX is "huh, it broke." T-91 only WARNs when Ollama fails; it doesn't catch the "Ollama is fine but embeddings aren't pre-warmed" case. (b) **Legacy-subject migration is two manual steps**: subjects created before T-84/T-92 have broken topic-vs-quality splits and bloated embedding texts. The fix is to click T-89's per-subject `Rebuild Rules` button on each subject, then run `pcca embed-backfill`. With ~5 subjects that's six discrete actions in the right order. Easy to get wrong, no guidance in the wizard. | (a) **Cold-cache guard at scoring time**: when `PCCA_SCORER` includes embedding and >50% of candidate items have NULL `content_embedding_json`, surface a notice in the scoring response and the wizard's failure box ("Embeddings not yet warmed — run `pcca embed-backfill` or `Backfill Embeddings` in Debug to enable embedding scoring") and silently prefer keyword for that run rather than hanging on lazy embed. Same shape as T-91's degraded-warning surface, but for cold-cache rather than failed-Ollama. (b) **Wizard `Rebuild All Subjects` action** in the Debug or Use tab: loops through every subject, calls the existing `/api/subjects/rebuild-rules` endpoint per-subject, then chains into `Backfill Embeddings`. Single click instead of N+1 manual steps. Safe to re-run (T-92's text-hash invalidation skips already-current embeddings). | (1) On a fresh DB with no embeddings warmed, clicking `Get Brief` with `PCCA_SCORER=both` does not hang — it scores via keyword and surfaces the "embeddings not warmed" notice. (2) After running backfill, the same click uses embedding scoring with no warning. (3) Wizard Debug has a `Rebuild All Subjects` action that loops rebuild-rules + backfill in one click. (4) The bulk action is idempotent — re-running it after a successful pass is fast and skips already-current items/subjects. | Closes the two remaining first-time-user friction points after T-87/T-90/T-91/T-92 closed the major ones. Without (a), a user hits a 5-30 min hang on their first Brief click and concludes the system is broken. Without (b), the legacy-subject migration is a six-step ritual that's easy to get wrong. Both are polish, not architecture — but they're the "first 5 minutes of dogfood" experience for anyone joining after T-87 ships. |
| T-96 | done | `services/youtube_transcript_service.py` requested transcripts only in English (`preferred_languages=("en",)`). Direct DB evidence (2026-05-01 audit): of 58 collected YouTube items, 28 had transcripts and 26 had none — and the gap split exactly along language lines. **English channels** (Andrej Karpathy 8/8, Lenny's Podcast 8/8, Claude and Anthropic 7/8, Michael Jackson 5/8) had transcripts. **Ukrainian channels** (STERNENKO 0/9, Vitaly Portnikov 0/9, Butusov Plus 0/8) had ZERO. The user confirmed transcripts ARE available via the YouTube UI for STERNENKO videos; we just never asked for them. Downstream impact: items without transcripts have `raw_text` ≈ 80-char title only, which collapses practicality / novelty / trust signals to ~0 — so even with embedding correctly identifying topical similarity, the composite score formula (`0.35·relevance + 0.30·practicality + 0.20·novelty + 0.15·trust − …`) puts a topically-perfect short-title item at ~0.21 while an off-topic Apple Podcasts item with rich description sits at ~0.61. Net effect: the entire Ukraine War News subject's top-5 was Apple Podcasts noise (Tim Berners-Lee, Preventative Care, NFL, etc.) despite the corpus containing 18 STERNENKO videos and 2 BBC pieces directly about Ukraine. | Reshape `YouTubeTranscriptService.get_transcript_text` to match yt-dlp's fallback chain. Use `YouTubeTranscriptApi.list(video_id)` to enumerate available transcripts, pick (in priority): (a) manual transcript in `preferred_languages`; (b) auto-generated in `preferred_languages`; (c) manual translatable transcript in any language → `translate("en")` → fetch; (d) auto-generated translatable transcript in any language → `translate("en")` → fetch; (e) any non-translatable transcript fetched in its original language. Translation produces English text that the (English-trained) `nomic-embed-text:v1.5` embedding can score against the user's English subject description without cross-language similarity loss. Errors (TranscriptsDisabled / NoTranscriptFound / RequestBlocked / IpBlocked) are treated as "no transcript right now" — the collector keeps making progress and the next collection run can fill the gap once the rate limit clears. | (1) New `tests/test_youtube_transcript_service.py` covers all five fallback rungs with an injected fake `youtube_transcript_api` module. (2) Manual English transcript wins over Ukrainian when both exist. (3) Manual Ukrainian without an English option is translated to English. (4) Auto-generated transcripts work the same way. (5) When translation isn't possible (e.g. `is_translatable=False`), the original-language text is returned rather than nothing. (6) Existing 26 missing transcripts will fill in on the next YouTube collection run once the YouTube transcript-API rate limit clears (today's audit pushed us into a temporary block). | Closes the largest remaining content-volume gap for non-English subjects. With Ukrainian transcripts translated to English, STERNENKO videos (currently scoring 0.21 against Ukraine War News) should clear ≥0.55 once the embedding sees a 5-50 KB English-translated body instead of an 80-char title. Combined with T-87's embedding-based relevance, this is what actually delivers "Ukraine War News briefs that are about Ukraine" — the goal of the 2026-05-01 dogfood loop. |
| T-97 | P2 | Today's YouTube data layer is two thin wrappers — RSS feed parser (T-86) for channel listings and `youtube_transcript_api` (T-96) for transcripts. Both are unofficial scrapers of YouTube's surfaces and both have failure modes we keep patching: RSS misses view/like deltas after a video ages, `youtube_transcript_api` rate-limits aggressively (we triggered the block during the 2026-05-01 audit), and neither honors the user's logged-in session, so private/age-gated/region-locked content stays invisible. The user's question "can we use my real browser session for YouTube?" surfaced the underlying ask: get richer data, including authenticated content, using credentials we already have via T-37 session capture. The browser-based path is wrong (T-86 proved YouTube serves a stripped page to headless Chromium regardless of cookies — fingerprint signal trumps auth signal), but the underlying need — *use our captured cookies, dodge anti-bot, get high-quality data* — is real. | **Replace both the RSS feed parser and `youtube_transcript_api` wrapper with `yt-dlp` as the single YouTube data source.** yt-dlp is the de-facto industrial-grade YouTube extractor: it handles all anti-bot evolution upstream of us, supports cookie auth via `--cookies-from-browser` (or a Netscape-format cookies file), and exposes both channel-level enumeration AND transcript download with language fallback + translation in one library. Concretely: (a) New `services/yt_dlp_service.py` thin wrapper around the `yt_dlp.YoutubeDL` Python API; uses a separate process pool so the synchronous library doesn't block the async pipeline. (b) YouTube collector's `collect_from_source` calls `yt_dlp_service.list_channel_videos(channel_id_or_handle, max_items=15)` returning a list of dicts with `external_id`, `title`, `description`, `published_at`, `view_count`, `like_count`, `duration_seconds`, `channel_name`. (c) Per-video `yt_dlp_service.get_transcript(video_id, prefer_languages=['en'], translate_to='en')` with the same fallback chain T-96 ships (manual preferred → auto-generated preferred → manual translatable → auto-generated translatable → original). (d) Cookies passed via the existing T-37C session-capture path: write captured cookies to `<browser_profile>/cookies.txt` in Netscape format on session refresh; pass that path as `cookiefile` to yt-dlp. (e) Drop `services/youtube_transcript_service.py` and the RSS feed parser in `collectors/youtube_collector.py` — both become dead code. (f) Optionally keep RSS as a fallback for the cookie-less / Ollama-down case. | (1) `yt_dlp_service` exists with `list_channel_videos` and `get_transcript` methods, both async-wrapped. (2) Per-video metadata includes `view_count` and `like_count` (RSS feed doesn't expose like_count). (3) Authenticated content (videos visible only to the logged-in account, age-gated, region-locked) is collected when cookies are present. (4) Transcript fetch on a Ukrainian video produces English text via yt-dlp's translation layer; rate-limit behavior is documented and gentler than `youtube_transcript_api`. (5) Existing tests for the YouTube collector (T-86) keep passing against the new fixture shape. (6) Live regression: the 26 currently-transcript-less items from STERNENKO / Vitaly Portnikov / Butusov Plus all gain English-translated transcripts on the next run. | T-96 is a tactical fix for the transcript-language gap; T-97 is the strategic consolidation that removes two homegrown YouTube wrappers in favor of one battle-tested library. Reduces ongoing maintenance against YouTube's anti-bot evolution (yt-dlp's maintainers track that for us), unlocks authenticated content the RSS path can't see, and is the architecturally correct answer to "use my real browser session for YouTube" — without actually opening a browser. P2 because T-96 covers the immediate need; T-97 is the right next step once T-96 is dogfood-validated. |
| T-98 | done | After T-87 (embedding-first scoring) shipped, the composite final-score formula `0.35·relevance + 0.30·practicality + 0.20·novelty + 0.15·trust − 0.20·noise_penalty` still let rich-body off-topic content beat topical-but-thin content. **Live evidence (2026-05-01 post-backfill audit on `AI Tools & Tips`):** Anthropic Mythos / Anthropic Claude Code video / Cat Wu (Lenny's) Anthropic-team interview — all directly on-topic per the user's description — scored 0.79-0.80. Above them: Ali Larter Hollywood interview (0.870) and "Darth Vader of Electric Utilities" climate piece (0.835). Embedding similarity correctly identified the Anthropic items as more relevant; the composite formula then weighted that relevance signal at only 35% and let practicality/novelty/trust (which favor long detailed Apple Podcasts descriptions regardless of topic) overrule it. The same shape was observed on `Ukraine War News` (Tim Berners-Lee biography at 0.609 vs. STERNENKO Ukraine-war commentary at 0.21) — but that one is also blocked on T-96 transcript backfill, so isolating L3 (composition) from L2 (body text) was hard until the AI Tools & Tips evidence arrived: same problem on a subject where every candidate has rich English transcripts already. | Rebalance the composite weights to make relevance the dominant signal. New defaults: `pass1_score = 0.7·relevance + 0.3·practicality`; `pass2_score = 0.55·relevance + 0.20·practicality + 0.15·novelty + 0.10·trust`; `final_score = 0.55·relevance + 0.20·practicality + 0.15·novelty + 0.10·trust − 0.20·noise_penalty`. Positive components still sum to 1.0. Embedding-based relevance is calibrated enough (T-87 shadow-A/B confirms the LLM rerank deltas are differentiating, not random) that we can lean into it. Practicality / novelty / trust still matter for tie-breaking among similarly-relevant items, but no longer override topical match. | (1) Test on the live DB: rescoring after the weight change moves on-topic Anthropic content above off-topic Hollywood/utility content for `AI Tools & Tips`. (2) `AI PM Success Stories` top-5 stays good or improves (it was already working under old weights — sanity check that the change doesn't regress it). (3) Existing `tests/test_curation_engine.py` keep passing under new weights. (4) Score range visibly widens — the 0.06 spread between top-5 items in `AI Tools & Tips` should expand once relevance is doing more work. | Closes the structural-composition gap that was the missing third leg after T-87 (intent capture) + T-96 (body text). The earlier worry that L3 might be self-correcting after L2 was wrong — proven via AI Tools & Tips where L1+L2 were already healthy and L3 still inverted the ordering. With T-98 the composite trusts embedding-based relevance proportional to how reliable it actually is. |
| T-99 | done | T-87 made embedding similarity the authoritative relevance signal at `pipeline/curation.py:85-88` (`if semantic_similarity is not None: relevance = embedding`). However, the legacy keyword-hit boost at lines 116-119 (`relevance = min(1.0, relevance + min(0.35, include_hits * 0.12))`) was kept and runs unconditionally — adding up to **+0.35** to relevance based on substring matches against `include_terms`. Result: embedding similarity is silently double-counted with keyword hits, and items that match generic include_terms by coincidence get artificially boosted past items whose embedding correctly identifies them as more relevant. **Live evidence (2026-05-01 post-T-98 rescore):** AI Tools & Tips top-2 collapsed to 0.001 score difference — "Darth Vader of Electric Utilities" at 0.824 over "How Anthropic uses Claude in Product Engineering" at 0.823. The Darth Vader piece picked up include-hits on `["leading", "companies", "tools"]` (Arizona's leading utility companies) worth ~+0.24 on top of its much-lower true embedding similarity, dragging it past the genuinely on-topic Anthropic content. T-87's design intent was for embedding to be the relevance signal; the include-hit boost was supposed to be a fallback for the keyword-only path. | Gate the include-hit boost on the absence of embedding similarity. New conditional at `pipeline/curation.py:117-127`: `if semantic_similarity is None: relevance = min(1.0, relevance + min(0.35, include_hits * 0.12))`. The exclude-hit penalty path is unaffected (excludes still feed `noise_penalty`, which is its own component, not part of relevance). When embedding is on, embedding similarity is the only thing that sets `relevance`; keyword hits on include_terms feed downstream signals (practicality/novelty/trust via the underlying text) but no longer compound on the relevance axis. Pure keyword scoring (T-87 shadow path with `PCCA_SCORER=keyword`) keeps the legacy include-hit behavior. | (1) Test on the live DB: rescoring after this change moves AI Tools & Tips on-topic Anthropic content above off-topic Electric Utilities. (2) AI PM Success Stories top-5 stays clean (it was working under the legacy keyword boost too, since the boost was a real signal there — sanity check that turning it off doesn't regress). (3) Keyword-only mode (`PCCA_SCORER=keyword`) is unchanged — the boost still applies when `semantic_similarity is None`. (4) Existing curation tests keep passing. | Closes the keyword-pollution leg of the relevance pipeline. T-87 + T-92 + T-98 + T-99 together are the four-part fix for "Briefs are irrelevant": (T-87) embedding identifies relevance correctly; (T-92) subject embedding uses the user's description, not bloated keywords; (T-98) composite weights trust relevance more than rich-body signals; (T-99) keyword include-hits stop polluting the embedding-derived relevance. With all four in place, the live AI Tools & Tips inversion should resolve and the path is clear for Ukraine War News once T-96 transcripts back-fill. |
| T-100 | done | User reported (2026-05-01) that the MacBook Pro got hot and the fan ramped during `pcca embed-backfill` runs. The heat source is Ollama doing dense Metal/Neural Engine inference, not pcca itself. Default `concurrency=4` for the embedding backfill ran 4 simultaneous embedding calls to `nomic-embed-text:v1.5` for ~10 minutes during the embed phase — that's sustained near-peak Metal load on a 13-15W chip with passive cooling under sustained workload. macOS doesn't expose cgroup-style core limits, so the practical lever is how many concurrent inference requests we fire at Ollama. The hardcoded default `concurrency=4` was tuned for fast backfill, not thermal safety. | Make backfill concurrency configurable via `PCCA_EMBEDDING_BACKFILL_CONCURRENCY` (default lowered from 4 to 2). Wire through `Settings.embedding_backfill_concurrency`, the CLI `--concurrency` flag (now defaults to settings value when not provided), and `DesktopCommandService.backfill_embeddings`. Old explicit `--concurrency=N` invocations still override. Trade-off: ~25-30% longer backfill duration for noticeably less thermal load and quieter fan. Users on plugged-in / actively-cooled rigs can set `PCCA_EMBEDDING_BACKFILL_CONCURRENCY=4` (or higher) to recover the original speed. | (1) Default backfill runs with concurrency=2. (2) `PCCA_EMBEDDING_BACKFILL_CONCURRENCY=4` restores the old default. (3) `pcca embed-backfill --concurrency=1` overrides the env var (most-conservative single-stream mode). (4) Existing tests pass. (5) The same lever applies whether triggered from CLI or wizard. | Closes the user-reported thermal complaint with a single env knob. The chip-level inference cost (per-call Metal load) doesn't change — only the parallelism. Doesn't fix the qwen2.5:7b Pass-2 rerank phase which is per-item-sequential by design (one heavy call at a time). For users who want even cooler operation: set `OLLAMA_NUM_PARALLEL=1` in the Ollama service env, which forces serial inference at the Ollama level system-wide. |
| T-101 | done | `pcca run_nightly_collection()` adds new items to the DB but doesn't embed them. Items sit cold until either (a) the user manually runs `pcca embed-backfill --include-segments`, or (b) `/briefs` lazily embeds during scoring (the cold-cache hang T-95 was filed for). New items therefore don't participate in scoring until a separate manual step is taken — easy to forget, breaks the Get Content → `/briefs` loop. **2026-05-02 user observation**: after the first-run heavy backfill (~30-90 min one-time), incremental backfill is bounded (~30s-2 min for typical per-platform collections after T-92's text-hash skip) — small enough to chain automatically without surprising the user. | After every successful `run_nightly_collection()` (Get Content button, `pcca run-nightly-once` CLI, or APScheduler nightly cron), the orchestrator chains `embed_backfill(scope=new_items_only, rescore=False, include_segments=True)`. **Embed-only** — rescore is deferred to `/briefs` (smart-rebuild path from T-67) or the morning_cron when `digest_auto_send=true`, because rescore is the time variable that's best done lazily. (a) **Trigger**: every collection, including per-platform Get Content. (b) **Wizard progress**: two-phase `"Collecting…"` → `"Embedding…"` indication on the Sources tab status box so the user knows the second phase is bounded and not a hang. (c) **Failure handling**: if embed fails (Ollama down, rate-limited), log WARN per T-91 and surface `embedding_pending=true` in the response, but return collection success — items are still useful via keyword scoring fallback. (d) **CLI parity**: `pcca run-nightly-once` and the morning_cron also chain. New `--no-backfill` flag (CLI) and `PCCA_AUTO_BACKFILL=false` (env) for users who want manual timing control. (e) **Scheduled morning rescore**: when `digest_auto_send=true` fires the morning_cron, run a full rescore before sending Briefs — that's what makes morning Briefs reflect overnight content. (f) **Locking**: chained backfill happens inside the same T-88 runtime lock as the collection, so a second click is still rejected by the lock and there's no concurrent-backfill race. | (1) After a per-platform Get Content for X, the new items have non-NULL `content_embedding_json` without a separate manual step. (2) Get Content responds with collection stats + an `embedding_pending` field — `false` on success or `true` if the embed step failed (Ollama down etc). (3) Wizard spinner shows the two-phase `Collecting… → Embedding…` indication. (4) `pcca run-nightly-once --no-backfill` skips the embed step (legacy behavior preserved). (5) `PCCA_AUTO_BACKFILL=false` disables auto-chain globally. (6) Scheduled morning_cron runs full rescore before sending Briefs. (7) Embed step skips already-embedded items via T-92 text-hash. (8) `/briefs` after collection reflects the new content immediately (rescore happens during `/briefs` or in morning_cron — either way it's bounded). (9) A failed embed step does not roll back the collection insert. | Closes the manual-step gap between collection and `/briefs`. Removes the cold-cache hang that T-95(a) was filed for (T-95 can be downsized after T-101 lands — the cold-cache guard becomes a defense-in-depth fallback rather than the primary mechanism). Makes the Get Content → `/briefs` loop self-contained — the user never has to remember to run backfill. Bounded by T-92's text-hash so incremental cost is small; the heavy first-run backfill is now structurally a one-time event. |
| T-102 | done | The wizard's Sources tab toolbar has a Platform dropdown that scopes both `Get Sources` and `Get Content` (per T-82). To collect new content from all platforms, the user has to click through each platform individually — 7+ clicks for a full sweep. Same for `Get Sources`. The underlying CLI (`pcca run-nightly-once` with no `--platform`) and the scheduled nightly already do all-platforms in one shot, but the wizard doesn't expose that path. **2026-05-02 user feedback**: requested an `"All"` option to trigger all-platforms with a single click. | Add an `"All"` option to the Platform dropdown (rendered as the first/default entry, above per-platform options). When selected: (a) `Get Content` triggers the unscoped collection path (`platform=None`, current legacy behavior preserved — same path the scheduled nightly uses). (b) `Get Sources` iterates over `SUPPORTED_ONBOARDING_PLATFORMS` sequentially and stages follows for each platform; per-platform progress shown in the Sources tab status box (`"Staging X… (3/7)"`). (c) Button label updates to `Get Content (All)` / `Get Sources (All)` when selected, matching the per-platform label convention from T-82. (d) Same T-88 runtime lock — only one collection / staging job at a time. (e) Sequential, not parallel: each platform opens a browser context and parallel contexts share resources poorly; serializing keeps thermal/memory bounded. | (1) Platform dropdown has an `"All"` option as the first/default entry. (2) Selecting `All` + clicking `Get Content` runs collection across all platforms (same path as `pcca run-nightly-once`). (3) Selecting `All` + clicking `Get Sources` iterates platforms sequentially with per-platform progress in the Sources tab status box. (4) Button label reflects the `All` selection. (5) Existing per-platform behavior preserved. (6) The T-88 runtime lock rejects a second click while the All-platforms run is in flight. | Reduces click count for full-sweep collection from 7+ to 1. Closes the gap where the underlying CLI/cron already supports all-platforms but the wizard doesn't. Especially useful for first-run dogfood when the user wants to populate the corpus broadly without per-platform clicking. |
| T-103 | done | T-101 review (2026-05-02) found three conditional code paths verified correct by reading but lacking explicit end-to-end tests: (1) `pcca run-nightly-once --no-backfill` actually skipping the embed step (the flag is wired through `cli.py:580-585` → `app.py:234-236`, but no test asserts the orchestrator received `auto_backfill=False`); (2) `PCCA_AUTO_BACKFILL=false` env var disabling the chain globally — config-layer unit test exists but no integration test verifying that orchestrator with `auto_backfill_embeddings=False` actually skips the embed phase during `run_nightly_collection()`; (3) pre-send rescore trigger — after T-105, `scheduler.py` calls `rescore_existing_items()` for both `morning_digest` and on-demand `/briefs` (`run_type="briefs"`); tests pin both paths. None of these are bugs; they're untested assertions about T-101's conditional paths that would catch a future refactor regression. | Add three integration tests to `tests/test_pipeline_orchestrator.py` and/or `tests/test_digest_feedback.py`: (a) `test_run_nightly_once_no_backfill_skips_embed` — assert orchestrator's embed phase is not invoked when `--no-backfill` is set. (b) `test_pcca_auto_backfill_env_false_disables_chain` — set `PCCA_AUTO_BACKFILL=false` via monkeypatch, run collection, assert no embedding occurred. (c) `test_morning_digest_and_smart_briefs_trigger_rescore` — pair test: morning_digest and on-demand Briefs both call `rescore_existing_items`. | (1) Three new tests added; all green. (2) Refactoring T-101's auto-chain logic in a way that breaks any of the three conditional paths now produces a test failure. (3) No coverage of legacy/regression paths affected. | Closes the test-coverage gap noted in the T-101 review without delaying dogfood. P3 because the conditional logic was verified correct by reading; this is regression insurance, not bug-finding. |
| T-104 | done | Current Telegram Brief format (`digest_renderer.py:152-158` + `telegram_service.py:141`) prioritizes debug noise over signal. The user-visible message reads: `📌 AI PM Success Stories\n\nBrief 1: <title>\nvia <author> - published <date>\n<url>\n\nWhy this matched: <full debug rationale string>\n\nMatched segment:\n[18:42] <raw segment text>`. **2026-05-02 user feedback** identified: (a) `📌 AI PM Success Stories` at the top is wrong placement — should be a `#AIPMSuccessStories` hashtag at the bottom for Telegram-side filtering. (b) `Why this matched: relevance=0.61, practicality=0.50, ...` dumps internal scoring rationale that the user doesn't need. (c) `Matched segment:` shows the verbatim segment text — user wants the **key idea/message rephrased by the LLM**, not the raw transcript chunk. (d) `Brief 1: <title>` puts the original title at the top — title should be lower (after the key idea). (e) **Possibly broken**: user reports YouTube source links missing time offset; `_brief_url` does append `?t=Ns` when `segment_start_seconds` is set, so the bug is either (i) `segment_start_seconds` is None in some items (e.g., short single-segment ones), (ii) the link is being shown but the user doesn't see it as a deep-link, or (iii) edge case in `_url_with_query` for YouTube URLs that already have query parameters. Needs verification. | **Redesign Brief layout** to lead with signal and demote chrome. New format: `<key_message>\n\n🎙 <author> — _<title>_\n[<timestamp>] · <duration> · <relative_date>\n<url_with_timestamp>\n\n#<SubjectHashtag>`. Buttons unchanged below. Concrete pieces: **(1) Key message** — extend Pass-2 batch rerank prompt (`model_router.py`) to also produce a `key_message` field per item: a 1-2 sentence rephrasing of the matched segment's core idea in the user's intent context (the prompt already has subject description + segment text; one extra JSON field is free). Persist `key_message` on `item_segment_scores` (or `digest_items`); fall back to first 200 chars of segment text if Pass-2 didn't run or returned empty. **(2) Subject as hashtag** — convert `subject.name` to a Telegram-friendly hashtag: strip non-alphanumeric, CamelCase, prefix `#`. e.g. `"AI PM Success Stories"` → `#AIPMSuccessStories`, `"Ukraine War News"` → `#UkraineWarNews`. Move from prefix in `telegram_service.send_brief_message` to suffix in the Brief body. Persist the computed hashtag on the subject (`subjects.telegram_hashtag` column) so refinement doesn't re-derive it. **(3) Source attribution** — `🎙 <author> — _<title>_` line: platform icon, author name, em-dash, italicized title. Platform icons: 🎙 podcast (apple_podcasts/spotify), 📺 youtube, 𝕏 x, 💼 linkedin, 📰 substack/medium, 🔗 rss/reddit. Title formatting via Telegram MarkdownV2 italic. **(4) Meta line** — `[<timestamp>] · <duration> · <relative_date>` where each is conditional: timestamp only for items with `segment_start_seconds`, duration only when known (YouTube/podcast), relative_date as "2 days ago" / "yesterday" / "today" / "3 weeks ago" computed from `published_at`. Drop em-dashes and use middle-dot `·` separator. **(5) URL** — inherit current `_brief_url` logic; verify the YouTube `?t=Ns` actually surfaces in production and add a regression test that a YouTube candidate with `segment_start_seconds=1122` produces `https://youtube.com/watch?v=XXX&t=1122s`. **(6) Drop user-facing rationale** — `"Why this matched: <rationale>"` removed from `short_text` and `full_text`; the rationale stays in `item_scores.rationale_json` for analytics/debug but is no longer rendered. **(7) Hashtag at bottom** — single line `#<SubjectHashtag>` separated by a blank line from the body. Telegram renders this as a clickable hashtag that filters all messages in the chat. **(8) MarkdownV2 escaping** — switch `send_brief_message` from plain text to `parse_mode=ParseMode.MARKDOWN_V2`, escape user-supplied content (titles, authors), and use `_…_` for italics, `*…*` for bold, `[label](url)` for links. **(9) Expand-button content** (`📖 More`): full segment text + the rationale (so debug info is reachable but not in the default view) + neighboring segments around the matched one if available. | (1) New Brief render produces format: `<key_message>\n\n🎙 <author> — _<title>_\n[<ts>] · <duration> · <rel_date>\n<url>\n\n#<Subject>`. Verified via fixture test on `tests/test_digest_renderer.py`. (2) `key_message` is 1-2 sentences rephrasing the matched segment's core idea (LLM-generated via Pass-2 prompt extension). Fallback to truncated segment text on miss. (3) Subject hashtag is computed as `_to_camel_hashtag(subject.name)` and persisted at subject creation/rebuild. CamelCase, alphanumerics only, `#` prefix. (4) Platform icon mapping: tested for all 9 platforms. (5) Relative date formatter: `"today"` / `"yesterday"` / `"N days ago"` / `"N weeks ago"` / `"N months ago"`. (6) Duration line shown only when present in `metadata.duration_seconds`. (7) `Why this matched:` no longer appears in the default Brief; rationale is only shown behind `📖 More` and persists in DB. (8) YouTube URL with `segment_start_seconds=1122` produces `https://www.youtube.com/watch?v=XXX&t=1122s` — explicit regression test. (9) Tap-📖-More expands to full segment text + rationale + neighboring segments. (10) MarkdownV2 escaping is correct: a title containing `_underscores_` or `*asterisks*` doesn't break the message. (11) Existing button row + reply detection from T-55/T-58 unaffected. (12) `tests/test_digest_renderer.py` updated to assert the new shape across the 5 subject types. | Turns Brief delivery from "engineering output dump" into "skimmable digest entry." The current format made the user actively unhappy; this is the surface they spend time with. Key changes by user impact: key_message replaces raw segment text (high — content-quality lift), hashtag at bottom enables filtering (medium — power-user quality of life), drop rationale (medium — removes confusion), source line with platform icon + italic title (low — but compounds with the rest into a readable card). YouTube timestamp link verification closes a possibly-real bug whose fix is one regression test plus any uncovered edge case. |
| T-105 | done | T-101 (auto-embed chain) was specced as *"embed-only at collection — rescore deferred to /briefs or morning_cron"*. The implementation correctly added embed-only chain (verified live: run_id=39 embed phase took 2 seconds for 34 changed X items at concurrency=2). But the **legacy per-subject scoring phase at the end of `run_nightly_collection()` was never touched** — it still runs Pass-2 LLM rerank across all 5 subjects regardless of whether the call was a full nightly or a per-platform Get Content. **Live evidence (2026-05-02 user-triggered Get Content for X)**: total wall time 9-10 minutes broken down as 75 sec collection + 2 sec embed (T-101 ✓) + ~8 min per-subject scoring (5 subjects × ~2 min Pass-2 rerank each). User experience: clicks "Get Content for X" expecting ~1-2 min, waits 10 minutes, the wizard's progress indicator stuck on "Embedding 34/34" the whole time because the JS doesn't surface the scoring phase. T-101 didn't deliver "fast Get Content" because scoring was never deferred at the orchestrator level. | Gate the per-subject scoring phase in `run_nightly_collection()` on a parameter (`score=True` default for backward compat with the scheduled nightly). Get Content from the wizard / `pcca run-nightly-once` calls with `score=False` so collection + embed return in seconds. **The per-subject scoring runs at the points T-101 already specced as the rescore moment**: (a) on `/briefs` (smart-rebuild path from T-67) which already runs scoring before Brief composition; (b) on `morning_cron` when `digest_auto_send=true`. Concretely: (i) Add `score: bool = True` parameter to `orchestrator.run_nightly_collection()`; when False, skip the `for subject in subjects: score_subject(...)` loop. (ii) `desktop_command_service.read_content` passes `score=False` when called from the wizard's Get Content button. (iii) `pcca run-nightly-once` passes `score=False` by default; new `--score` flag for callers that want the legacy behavior. (iv) The scheduled nightly cron path (APScheduler) keeps `score=True` for backward compat — overnight scoring is fine. (v) The morning_cron rescore path from T-101 is unaffected. (vi) Telegram `/briefs` smart-rebuild path was already triggering scoring; that becomes the user-visible scoring trigger. | (1) Wizard Get Content for X completes in ~75-90 seconds (collection + embed only); user-visible spinner clears within that window. (2) `pcca run-nightly-once` (no `--score`) completes in seconds-to-minutes (collection + embed). (3) `pcca run-nightly-once --score` runs the legacy collect+embed+score path. (4) `/briefs` after a Get Content includes the new items in scoring (smart rebuild detects new candidates per T-67 + scores them inline). (5) Scheduled nightly cron still runs full scoring (no regression for users with `digest_auto_send=true`). (6) Test: regression test pinning that `score=False` skips the per-subject scoring loop. | Closes the gap between T-101's spec and reality. The user's "10 minute Get Content" experience becomes the "75 second" experience the design promised. Defers the expensive Pass-2 rerank to `/briefs` (which already wraps it in smart-rebuild) or morning_cron, where the user expects to wait. P1 because it directly affects the user-facing latency on every Get Content click. |
| T-106 | done | T-101 added a "two-phase progress" indication in the wizard (`"Collecting…"` → `"Embedding…"`) but the wizard's progress callback at `desktop_command_service.py:1102-1110` only emits events for the `collecting` and `embedding` phases. The legacy per-subject scoring phase at the end of `run_nightly_collection()` runs silently — no progress event, no log surface in the wizard's status box. **Live evidence (2026-05-02)**: user clicked Get Content for X, saw "Embedding 34/34" indefinitely while the orchestrator was actually 8 minutes into the per-subject Pass-2 scoring phase. Even after T-105 lands and Get Content stops triggering scoring inside its call, the same UX gap exists for `/briefs` (which DOES trigger scoring) and the scheduled nightly. | Extend the orchestrator's progress callback to emit a `"scoring"` phase event with per-subject granularity. Each subject scoring start emits `{"kind": "scoring", "subject_index": N, "subject_total": M, "subject_name": "..."}`. Wizard JS in `server.py` updates the inflight status label to `"Scoring AI PM Success Stories (3/5)…"`. Same shape as the existing collecting/embedding progress. Apply both to wizard's Backfill Embeddings action and to any future `/briefs`-triggered scoring run that surfaces in the wizard's failure box. | (1) During scoring, wizard label shows `"Scoring <subject> (<n>/<total>)…"` with current subject name. (2) Telegram `/briefs` rescore path emits the same events to its log/run_logs metadata so it's traceable in `pcca debug-bundle`. (3) Existing tests still pass; new test asserts the `scoring` phase event is emitted when a subject is being scored. | Closes the visibility gap that made Get Content feel hung when it was actually working. Smaller user impact than T-105 (which removes the wait entirely), but worth doing for `/briefs` and scheduled-nightly observability — a 10-minute scoring run with no progress is bad UX even when expected. |
| T-107 | done | T-104 added a `#<SubjectHashtag>` line at the bottom of every Brief and switched `telegram_service.send_brief_message` to `parse_mode="MarkdownV2"`. The hashtag was appended unescaped at `digest_renderer.py:247`: `parts.extend(["", subject_hashtag])`. **MarkdownV2 reserves `#`** — every Brief send returned HTTP 400: `Can't parse entities: character '#' is reserved and must be escaped with the preceding '\'`. **Live regression (2026-05-02)**: Brief deliveries failed 100% across both AI Tools & Tips (log 12:02:50, item 18) and AI PM Success Stories (log 12:09:56, item 18). User-visible symptom: Telegram bot says `"Get Briefs for <subject> completed"` but no Brief messages ever arrive in the chat. | Wrap `subject_hashtag` in `escape_markdown_v2()` at `digest_renderer.py:247`. Rendered form becomes `\#AIPMSuccessStories`; Telegram's hashtag detector runs after MarkdownV2 parsing and still recognizes `\#word` as a clickable hashtag, so user-visible behavior (clickable, filterable hashtag) is unchanged. | (1) New regression test `test_telegram_renderer_escapes_hashtag_for_markdown_v2` asserts the escaped form appears and no bare `#` remains in `short_text` or `full_text` after stripping escaped sequences. (2) Existing 14 digest-renderer tests still pass. (3) Live verification: `/briefs` for AI PM Success Stories now delivers Brief messages to the chat. | Closes the 100%-Brief-delivery-failure regression introduced by T-104. One-line fix; the rest is regression test + clear commit message so the failure mode is documented if a similar escape gets dropped in the future. |
| T-108 | done | YouTube collection failed 5 of 7 sources with `httpx.HTTPStatusError: 404 Not Found` on the public RSS feed (`https://www.youtube.com/feeds/videos.xml?channel_id=UCXUPK…` and four similar UC-ID URLs in the 2026-05-02 12:07 run). Channels 6-7 were then skipped by the T-77 circuit breaker after the threshold tripped. **Result: 0 YouTube items collected.** This is **not** a session/cookie problem — the YouTube RSS feed is public and ignores cookies. The 404 means the channel IDs in `sources.account_or_channel_id` either (a) don't exist (deleted/renamed), (b) are malformed (bad resolution from `@handle` at follow-import time), or (c) point to private channels with disabled RSS. The wizard's failure handler conflated this with a session-class error and prompted the user to *"re-build session in browser"* — a misleading prompt for an RSS-side 404. The user re-captured cookies (log 12:07:39-44, `Session refresh complete platform=youtube`) and the next run STILL failed because cookies aren't the issue. | **Two parts**. (a) **Session-repair prompt should fire only on session-class errors** (HTTP 401/403 on authenticated paths, login-redirect, `SessionChallengedError`), not on RSS 404. The wizard / desktop_command_service classifies collector failures: `bot_shaped` → maybe re-capture; `not_found` (404 on RSS) → propose **re-resolve channel ID** instead of session repair. (b) **Channel-ID validation + re-resolution**. On a 404 RSS response for YouTube, the collector calls `resolve_source_identifier()` once with the original handle/identifier (if available in `sources.metadata_json.original_handle` from follow-import), gets a fresh UC ID via the `/about` page scrape, persists the new ID, and retries. If re-resolution also yields 404, mark the source as `follow_state='inactive'` rather than retrying every nightly. The wizard's Sources tab surfaces the inactive state with a "Channel not found — was it renamed or deleted?" hint. | (1) An RSS 404 for a YouTube source no longer triggers the "re-build session" prompt. (2) On 404, the collector attempts a one-shot UC-ID re-resolution via the original handle. (3) If re-resolution succeeds, the new ID is persisted and the scrape retries with the new feed URL. (4) If re-resolution also yields 404, the source is marked inactive and is not retried in subsequent nightly runs until the user manually reactivates. (5) Wizard Sources tab shows `inactive — channel-not-found` for affected rows with a "Re-add or remove" action. (6) Test fixture: 5 of 7 YouTube sources return 404; first re-resolution succeeds, source 2 is permanently 404 and gets marked inactive. | Closes the misleading-prompt UX issue and the actually-broken YouTube collection. Two-step fix: distinguish error class so the wizard guidance is truthful, then add the re-resolution path so transient handle→UC drift self-heals. P1 because YouTube is currently 0/7 producing items and the user's testing depends on YouTube content (Karpathy, Lenny's, Anthropic, STERNENKO etc.). |
| T-110 | done | Wizard actions taking longer than ~60 seconds **always** fail on the JS side with `"Load failed"`, even when the backend completes successfully. Root cause: PyWebView wraps WKWebView, which uses `URLRequest` whose `timeoutInterval` defaults to **60 seconds** for an idle connection. The JS-side `AbortController` + `timeoutMs: 1800000` (30 min) configured in `server.py:301-314, 338, 372, 377, 381, 386` does NOT override WebKit's underlying timeout. **Live evidence (2026-05-02 12:00-16:07)**: every long-running action affected — LinkedIn Get Sources at 67/77/84s (count=11, `ok=True` × 3), YouTube Get Content at 96/100/110s (48 items, `ok=True` × 3) — all triggered "Load Failed" alerts despite backend success. User experience: click action → wait 60s → "Load Failed" / "Try session repair" → retry → same outcome. T-83's `timeoutMs: 1800000` was the right idea on paper but is silently overridden by WebKit. T-72 was a closely-related earlier instance solved by making the action complete in <60s (iTunes lookup caching); for actions that fundamentally exceed 60s (LinkedIn headful scrape, YouTube full-corpus collection, embedding backfill, scoring), a different shape is required. | **Convert all long-running wizard actions to fire-and-poll.** Click handler returns immediately with HTTP 202 + `action_id`; the wizard's existing `/api/state` polling at line 595 already surfaces `inflight_actions`. When the action_id no longer appears in `inflight_actions`, the wizard fetches the result via a new `/api/actions/<action_id>/result` endpoint that returns the cached final response. Concrete pieces: (a) `_run_guarded_action()` in `desktop_command_service.py` already creates an `asyncio.Task`; extend `InflightAction` with a `result_future` so the dispatched task's result can be awaited later. (b) Add `/api/actions/<id>/result` endpoint that returns the action's final `CommandResult` (or 404 if not finished, 410 if too old). (c) Replace `await postAction(...)` in JS for `getSources()`, `readContent()`, `backfillEmbeddings()`, `getBrief()`, `rebuildAllSubjects()` with a "kick" call that returns 202 + action_id, then a polling loop that watches `state.inflight_actions` until the key disappears, then fetches the cached result. (d) Wizard's status box continues to show the existing `inflight_actions` label so progress events from T-106 are still visible. (e) Cache action results for ~5 min so a polling client can fetch them after completion. (f) **Alternative cheaper fix** if the full refactor is risky: server-side streaming response that writes a heartbeat byte every 30s during long actions to keep the WebKit connection alive. Doesn't need new endpoints; the existing fetch keeps receiving data so WebKit's idle timeout doesn't fire. Less robust but small change. | (1) Wizard click on Get Sources (LinkedIn, ~80s) does NOT show "Load Failed" or any session-repair prompt; instead the wizard label transitions through the existing T-106 progress events and resolves to a success notice. (2) Same for Get Content (YouTube, ~100s), Backfill Embeddings (~30 min one-time), Get Brief on cold cache, full nightly run from wizard. (3) Tests: simulate 90s server response; assert the wizard fetch returns 202 immediately with action_id, and the polling path correctly retrieves the result. (4) Backward compat: existing `postAction()` callers for short actions (<60s) still work as before. (5) The cached action result endpoint returns 410 for action_ids older than 5 min so polling clients don't accumulate stale results. | Closes the show-stopper UX bug that makes the wizard unusable for any action exceeding 60 seconds. Currently: 3 LinkedIn Get Sources runs and 3 YouTube Get Content runs today all succeeded server-side but appeared failed to the user, who then re-ran them, wasting time and burning anti-bot budget on the source platforms. Fire-and-poll also enables future actions of any duration (segment backfill across larger corpora, future heavy operations) without UX regression. P0 because every wizard test session right now is dominated by misleading "Load Failed" popups. |
| T-111 | done | Wizard's `getSources()` error handler at `server.py:349` shows the session-repair confirm prompt on **every** fetch error, with no classification: `const shouldRepair = confirm("${err.message}\n\nTry a session repair from your local browser, then import again?")`. T-108 fixed error classification at the orchestrator level (distinguishing `bot_shaped` / `not_found` / `empty_legitimate`), but the wizard JS never gets to read the orchestrator's `failure_class` because (a) when fetch succeeds the response carries `ok=True` (not an error) and (b) when fetch fails from the WKWebView 60s timeout (T-110), there's no body to read. So the prompt fires on transport errors that have nothing to do with the session. **Live impact (2026-05-02)**: user got the session-repair popup three times today on LinkedIn Get Sources runs that succeeded server-side; each time the suggested "session repair" would have just re-captured the same valid cookies and re-run the same flow. Misleading + wastes time. | **Two-step**. (a) After T-110 lands and the wizard reliably reads backend responses, classify error responses by `failure_class`: only show the session-repair confirm when the backend response carries `failure_class: "session_challenge"` or HTTP 401/403 from a session-class endpoint. For transport / unknown errors, show a neutral "Action did not finish — check Logs" toast without prompting for session repair. (b) Use the wizard's status box (existing T-106 progress events) to show in-progress and final state; reserve modal `confirm()` for actions that genuinely require user decision. | (1) A successful Get Sources for LinkedIn does NOT show the session-repair prompt. (2) A simulated session_challenge backend response DOES show the prompt. (3) A simulated transport error (server unreachable) shows a neutral "Action did not finish" notice, not a session prompt. (4) Existing T-37C inline session repair flow on missing cookies still works. | Removes the misleading session-repair prompt that makes users think the session is broken when actually the backend succeeded. Smaller user-impact than T-110 (which removes the underlying transport timeout entirely) but compounds with T-110: even after T-110 makes the action succeed visibly, leaving the unconditional session-repair prompt in `getSources()` would still fire on real future failure modes that aren't session-class. P2 because it depends on T-110 to land first. |
| T-112 | done | Wizard JS error handling is opaque from the user's perspective: when a fetch fails with `"Load failed"` (T-110 root cause), the JS at `server.py:323` calls `alert(err.message)` and `logLine("ERROR: ${err.message}")`. The error message is just the underlying browser exception (`Load failed` / `Failed to fetch` / `AbortError`) with no context: which action, how long it ran before failing, what the response status was, what the request URL was. When the user reports a wizard issue, we can't easily correlate their "Load Failed" with a specific backend run because the wizard never tells the backend it saw the error. Today's diagnosis required cross-referencing screenshots' timestamps with `pcca.log` lines manually — a luxury we won't always have. | Add a `/api/debug/wizard-events` POST endpoint that accepts JSON `{timestamp, action_key, action_id, event_kind: "fetch_error" \| "timeout" \| "success", elapsed_ms, http_status?, error_type?, error_message?}`. Wizard JS in `server.py` `request()` and `postAction()` posts to this endpoint on fetch failures and on response receipt for long actions. Server logs the event at INFO level with a `[wizard]` tag and persists last 200 events in a small `wizard_events` table for `pcca debug-bundle` to include. **No PII** beyond what the user voluntarily reports; no fetch-success events for short actions to keep volume low. | (1) Wizard fetch failures emit a `wizard_events` log line with action_id and elapsed_ms — easy to correlate with backend run via the same action_id. (2) `pcca debug-bundle` includes the recent wizard_events rows in the bundle. (3) The endpoint accepts up to 1KB per event and rejects oversized bodies. (4) Browser exception types (`AbortError`, `TypeError`, etc.) are recorded distinctly so we can confirm the WKWebView idle timeout vs. real network errors in future diagnoses. | Closes the post-hoc-diagnosis gap that ate hours today reading 5 rotated logs to confirm what the wizard JS already knew (60s, fetch aborted, AbortError). Cheap to add; valuable every future wizard regression. P3 because it's diagnostic plumbing — once T-110 lands, fetch failures should become rare in practice; this insulates against the next class of wizard issues. |
| T-113 | done | **Subject `description_text` is corrupted on 3 of 5 subjects.** Stored text is the LLM rebuild **prompt scaffold**, not the user's actual description. Live evidence (2026-05-02): `Ukraine War News.description_text` = `"Subject: Ukraine War News\nCurrent include terms: reputable sources, high quality analytics, key thoughts, facts\nCurrent avoid terms: propaganda, rumors, gossip\nRebuild this into literal topic terms th[at]..."` — that's the text fed INTO the LLM during rebuild, not the LLM's output and not the user's original input. Same shape on `AI impact over IT jobs market` and `AI Tools & Tips`. `AI PM Success Stories` and `Ukrainian Sole Proprietorship Laws` have empty `description_text`. **Per T-92, the subject embedding is computed from `description_text`** — so the embedding for Ukraine War News is built from words like `"Subject", "Current include terms", "Rebuild", "topic"` (programming/meta vocabulary). That embedding is closest to Karpathy's GPT tutorials and farthest from STERNENKO Ukrainian war commentary. The 2026-05-02 14:49 Brief delivery confirms: top-5 Ukraine War News = 4× Karpathy AI tutorials + 1× BBC AI Mythos, zero Ukraine content. The rebuild flow (T-89 wizard button + T-84 prompt) accidentally stores the prompt-builder text BACK into `description_text` instead of the user's input. | (a) Fix `desktop_command_service.rebuild_subject_rules()` to **preserve `description_text` verbatim** across rebuild operations — only `subject_preferences` (rules) get rewritten, never `subjects.description_text`. (b) Add `pcca repair-subject-descriptions` CLI taking JSON `{<subject_id>: "<original description>"}` to manually restore corrupted descriptions. (c) Regression test asserting `description_text` is unchanged before/after rebuild call. (d) Force re-embed of repaired subjects (text-hash invalidation triggers on next backfill). | (1) Re-running rebuild on a healthy subject does not modify `description_text`. (2) `pcca repair-subject-descriptions` writes user-provided text into the right rows. (3) Subject embedding recomputed on next backfill after repair. (4) Live verification: after repair, `Ukraine War News` Briefs include actual Ukraine content in top-5 (assuming Ukrainian transcripts are also fixed per T-114). (5) Regression test in `test_desktop_command_service.py` pins `description_text` invariance through a rebuild cycle. | Actual reason Briefs feel "irrelevant" across 3 of 5 subjects today. The rebuild flow has been silently corrupting the most important input every time the user clicks `Rebuild Rules`. Without fixing this, every other downstream improvement operates on a broken signal. P0 because it makes 60% of subjects produce nonsense Briefs. |
| T-114 | done | The 34 already-collected Ukrainian YouTube items (STERNENKO 9, Vitaly Portnikov 9, Butusov Plus 8, plus a few BBC/Shawn Ryan crossover hits) all have `transcript_text=NULL`. T-96 fixed `YouTubeTranscriptService` to request transcripts in any language and translate non-English to English, but T-96 only takes effect for **newly-collected items** — the 34 historical items are stuck with original NULL transcripts. With no body text, these items cannot compete on segment-level embedding similarity for any subject. Net: Ukraine War News has no Ukraine body text to surface, compounding T-113's description_text corruption. | Add `pcca youtube-rebackfill-transcripts` CLI iterating `items WHERE platform='youtube' AND (transcript_text IS NULL OR LENGTH(transcript_text) < 100)`, calling new T-96 multi-language `YouTubeTranscriptService.get_transcript_text(video_id)` for each, updating `transcript_text` and `raw_text`, bumping `content_hash` to trigger re-embed on next backfill. Bounded concurrency (`PCCA_YOUTUBE_TRANSCRIPT_BACKFILL_CONCURRENCY`, default 2) to stay below youtube-transcript-api rate limit. Resumable on `RequestBlocked`/`IpBlocked`; already-fetched items skipped via >100 chars check. | (1) Running `pcca youtube-rebackfill-transcripts` updates the 34 Ukrainian items with English-translated transcripts. (2) `raw_text` and `content_hash` change so next `embed-backfill` re-embeds the affected items. (3) After re-embed, Ukraine War News Briefs include actual STERNENKO/Portnikov segments with timestamps. (4) Idempotent: re-running skips already-transcripted items. (5) Rate-limit failure exits cleanly; partial progress preserved. | Closes the gap T-96 left by only fixing forward — historical items in NULL-transcript state contribute nothing. Without this, T-113's subject-description fix still doesn't recover Ukraine relevance because the corpus has no Ukraine body text. P1 because it pairs with T-113 to get Ukraine War News producing a real top-5. |
| T-115 | done | Two YouTube items with body text 7989-7990 chars trip Ollama's `nomic-embed-text:v1.5` ~8K input cap and return HTTP 500. `EmbeddingService` logs WARNING and returns None; orchestrator counts these as `items_failed`. Live evidence (2026-05-02 02:26, 15:59): 4 embedding failures total today, all text_chars 7989-7990. Items stay un-embedded; segments still get embedded (segments are shorter), so impact is small but the 500 fires every auto-backfill against these items. | Pre-truncate text at the embedding-service layer. `EmbeddingService.embed(text)` clips `text` to `min(len(text), MAX_EMBED_CHARS)` where `MAX_EMBED_CHARS=7500` default (env `PCCA_EMBEDDING_MAX_CHARS`). Truncation prefers paragraph/sentence boundary if found within last 100 chars; otherwise hard-cut. Logs at INFO when truncation fires. | (1) Items with body text >7500 chars no longer trip Ollama 500. (2) `content_embedding_json` populated. (3) `PCCA_EMBEDDING_MAX_CHARS=8192` env lets users with larger model raise cap. (4) Truncation logged at INFO. (5) Test: 9000-char input embeds successfully producing same embedding as input truncated to 7500 chars. | Closes a small but persistent error-spam path. P3 because impact is tiny — clutter reduction. |
| T-116 | done | T-104 acceptance #2: *"key_message is 1-2 sentences rephrasing the matched segment's core idea (LLM-generated via Pass-2 prompt extension)"*. Pass-2 prompt extension exists at `model_router.py:69-72`; persistence at `item_scores.py:77-126`; retrieval at `digest_renderer.py:142,189,232`. **In practice, 0 of 25 most-recently-delivered Brief items have populated `key_message`**. SQL audit on `digest_items JOIN item_segment_scores`: of all 11,159 segment_scores, only 194 (1.7%) have non-empty `key_message`; none of those 194 are items topping current Briefs. Renderer hits `_key_message()` fallback at `digest_renderer.py:275-282` 100% of the time → returns `(segment_text or title)[:260]` → exactly the "raw text cut out of context" the user observed (Karpathy's Brief opens with `"Let's build the GPT Tokenizer The Tokenizer is a necessary..."`, Lenny's opens mid-sentence with `"thing that I now know is possible to do..."`). Pass-2 batch rerank either isn't producing key_message for items that end up in Briefs, or result isn't persisted on the right `(item_id, subject_id, segment_id)` tuple, or smart-rebuild path bypasses Pass-2. | Three diagnostic + repair steps. (a) **Add INFO logging** at `model_router.rerank_batch()` recording `key_message_count` (how many returned items had non-empty key_message) and `top_k_count` (how many segments sent). Run a Brief delivery and read the log to see whether Pass-2 produces key_message at all. (b) If Pass-2 produces them but they're not persisted on Brief items, audit persistence: `upsert_segment_score()` must be called with `key_message` for the **same `(item_id, subject_id, segment_id)`** that ends up in the Brief. There may be a join mismatch where rerank returns key_message keyed on `item_id` but persistence keys on `segment_id`. (c) If persistence is correct but smart-rebuild bypasses Pass-2 rerank with prompt extension, fix `rescore_existing_items()` to invoke the same Pass-2 batch path used by `run_nightly_collection()` when `score=True`. | (1) Diagnostic log shows `key_message_count=N/M` per rerank batch, non-zero. (2) After a `/briefs` rescore, top-5 items per subject have non-NULL `key_message`. (3) Brief `short_text` shows the LLM-rephrased key_message, not raw truncated transcript. (4) Fallback `_key_message()` invoked only for items genuinely without Pass-2 result. (5) Regression test simulates rerank batch with 3 items, asserts all 3 land in `item_segment_scores.rationale_json.key_message`. | Closes the user-visible "Briefs look like raw text cut out of context" complaint. T-104 was specced for this and wiring exists, but production shows 0/25 coverage on Brief items — diagnostic log is cheapest way to find out which step fails. P1 because Brief readability is the user-facing product. |
| T-117 | done | `📖 More` expansion shows the full matched segment as **raw transcript text**. For a 5-min podcast/video segment that's typically 700-2500 words of verbatim transcription including fillers ("uh", "you know"), restarts, repetitions. **User feedback (2026-05-02)**: even the full segment "I'd actually prefer to be slightly refined by the model." Today's `_brief_full_text()` at `digest_renderer.py:251-269` calls `_segment_body(candidate, limit=full_text_chars)` — straight pass-through of raw `candidate.segment_text` truncated to per-subject `brief_full_text_chars` cap (default 1800). No LLM polish. | Extend Pass-2 prompt to also produce `refined_segment` (3-6 sentences) — same content cleaned up: drop fillers/repetitions, keep concrete claims/quotes/numbers, preserve speaker context. Persist alongside `key_message` in `item_segment_scores.rationale_json`. Renderer `_brief_full_text()` uses `refined_segment` when present, falls back to existing raw `_segment_body()` when not. User still gets raw transcript via source URL deep-link. | (1) Pass-2 prompt produces both `key_message` and `refined_segment` per item. (2) `refined_segment` typically 3-6 sentences; `MAX_REFINED_CHARS=1500` cap. (3) Brief `full_text` shows `refined_segment` followed by source attribution instead of raw transcript chunk. (4) Test: `refined_segment` rendered in `full_text` for an item with Pass-2 result; raw segment rendered for items without. (5) `📖 More` becomes a readable paragraph instead of a transcript dump. | Closes the second half of the user's "Briefs look like raw text" complaint. T-116's `key_message` is for the short body; T-117 adds same model treatment to expanded body. Bounded by same Pass-2 budget — no new LLM calls, just more output per existing call. P2 because it depends on T-116 wiring being correct first. |
